# STEP 2.2 — Cập nhật API route `src/app/api/system/config/route.ts`

## Mục tiêu
Chuyển đổi các endpoint `GET` và `POST` của `/api/system/config` sang sử dụng các hàm async DB persistence từ `src/lib/system-config.ts`.

## Trạng thái
⬜ Chưa bắt đầu
