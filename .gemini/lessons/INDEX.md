# Lessons Learned — Kho Kinh Nghiệm Toàn Cục

> Áp dụng cho **tất cả tài khoản** sử dụng máy này — bất kể đăng nhập bằng tài khoản nào.
> Mỗi session BẮT BUỘC kiểm tra mục liên quan trước khi thực hiện task.

---

## Quy tắc sử dụng

| Thời điểm | Hành động bắt buộc |
|---|---|
| Trước khi bắt đầu tính năng mới | Glob `lessons/<category>/` → đọc lesson liên quan |
| Khi gặp lỗi / bug | Glob `lessons/<category>/` → tìm pattern tương tự |
| **Fix được bug trong quá trình làm** (> 15 phút tìm) | **Ghi lesson NGAY** — không đợi hết task |
| Phát hiện "gotcha" / hành vi bất ngờ giữa chừng | **Ghi lesson NGAY** — không đợi hết task |
| Hoàn thành tích hợp SDK / thư viện lần đầu | Ghi lesson sau khi xong bước đó |
| Verify / kiểm tra thấy điều đúng không hiển nhiên | Cập nhật hoặc tạo lesson ngay |
| Lesson cũ sai / lỗi thời | Cập nhật file, ghi chú `updated:` |

> ⚡ **Nguyên tắc cốt lõi:** Ghi **trong lúc đang làm**, không phải sau khi xong. Session đóng = kinh nghiệm mất.

---

## Danh mục

| Category | Thư mục | Mô tả |
|---|---|---|
| C# WinForms | [csharp-winforms/](csharp-winforms/) | Kinh nghiệm với Windows Forms, controls, events |
| Avalonia UI | [avalonia/](avalonia/) | Migration từ WinForms, Avalonia-specific patterns |
| .NET General | [dotnet-general/](dotnet-general/) | DI, config, async, exception, NuGet |
| Camera Integration | [camera-integration/](camera-integration/) | SDK camera, RTSP, LPR, Milesight, Hikvision |
| Networking & Protocol | [networking-protocol/](networking-protocol/) | Modbus, TCP, Serial, socket patterns |
| Database | [database/](database/) | EF Core, SQL, migration, transaction |
| UI Patterns | [ui-patterns/](ui-patterns/) | Layout, binding, MVVM, navigation |
| Windows Tooling | [windows-tooling/](windows-tooling/) | PowerShell, encoding, junction/symlink, script hệ thống |
| Python & Tkinter | [python-tkinter/](python-tkinter/) | App Tkinter, hiệu năng import, đóng gói PyInstaller, ML tooling |
| React & Web | [react-web/](react-web/) | React 19, Vite, TypeScript, thư viện web (KaTeX/MathLive), bundle & test |
| Linux Desktop | [linux-desktop/](linux-desktop/) | GNOME, dconf/gsettings, kiosk mode, X11/Wayland, systemd desktop |
| Computer Vision | [computer-vision/](computer-vision/) | OpenCV, threshold/segmentation, license-plate/OCR tooling |

---

## Index theo lesson

### dotnet-general/
- [signalr-object-vs-primitive-args-mismatch.md](dotnet-general/signalr-object-vs-primitive-args-mismatch.md) — `SendAsync("Event", new { field })` → JS nhận toàn bộ object, không phải field value; phải gửi primitive trực tiếp `SendAsync("Event", value)` hoặc nhiều arg `SendAsync("Event", a, b, c)` để khớp handler `function(a,b,c)`

### computer-vision/
- [opencv-otsu-fails-on-full-plate-photo-with-margin.md](computer-vision/opencv-otsu-fails-on-full-plate-photo-with-margin.md) — Otsu global threshold ra 0 box khi ảnh còn nền trắng/viền quanh biển số (histogram đa mode); adaptiveThreshold blockSize sai lại làm ký tự rỗng ruột. Fix: định vị biển số bằng HSV color mask trước, Otsu lại trong vùng biển đã crop sạch

### linux-desktop/
- [xlib-concurrent-taskrun-abandon-domino-block.md](linux-desktop/xlib-concurrent-taskrun-abandon-domino-block.md) — "Bỏ qua task chậm, bắn Task.Run mới" khi gọi vào CÙNG 1 Xlib Display gây domino: task mới chỉ xếp hàng chờ khoá nội bộ Xlib, tự tính đủ timeout rồi bị coi là "chậm" tiếp — burst 66-118 dòng warning liên tiếp. Fix: LUÔN await task cũ xong thật trước khi lấy lệnh kế tiếp
- [lubuntu-lxqt-kiosk-lockdown-differs-from-gnome.md](linux-desktop/lubuntu-lxqt-kiosk-lockdown-differs-from-gnome.md) — Lubuntu/LXQt: Openbox user config là `~/.config/openbox/rc.xml` (không phải `lxqt-rc.xml`); phím tắt thoát kiosk THẬT (Ctrl+Alt+T terminal, Meta+E file manager, Meta+R runner, Super_L menu) nằm ở daemon riêng `lxqt-globalkeyshortcutsd` (`~/.config/lxqt/globalkeyshortcuts.conf`), không phải Openbox rc.xml
- [gnome-wayland-remote-control-mutter-dbus-pipewire.md](linux-desktop/gnome-wayland-remote-control-mutter-dbus-pipewire.md) — Remote control kiosk không người trực trên GNOME Wayland: dùng Mutter private D-Bus API (ScreenCast+RemoteDesktop) thay xdg-desktop-portal (portal luôn hiện dialog xin phép); capture PipeWire qua subprocess gst-launch-1.0 thay native binding; pairing session phải đúng thứ tự; input evdev code/axis khác XTest; Tmds.DBus vá lỗ hổng chỉ từ 0.92.0 (0.21.3 là version của package khác); installer hardcode `XDG_SESSION_TYPE=x11` phá Wayland detection
- [dconf-system-db-profile-required.md](linux-desktop/dconf-system-db-profile-required.md) — dconf system-db + locks bị bỏ qua âm thầm nếu thiếu `/etc/dconf/profile/user` (Ubuntu không có sẵn); session đang chạy chỉ nhận profile mới sau re-login/reboot. Gesture 3 ngón mở overview không tắt được bằng gsettings
- [deb-package-app-icon-missing.md](linux-desktop/deb-package-app-icon-missing.md) — Cài .deb xong không có icon: `.desktop` dùng đường dẫn tuyệt đối (typo → DE im lặng bỏ qua) + gói thiếu icon trong `/usr/share/icons/hicolor`; đừng dựa vào `postinst` gọi `runuser install.sh`. Check nhanh: `dpkg -c file.deb | grep icons/hicolor`
- [dotnet-version-requires-sdk-not-runtime.md](linux-desktop/dotnet-version-requires-sdk-not-runtime.md) — `dotnet --version` luôn lỗi/NOT_FOUND trên máy chỉ cài .NET Runtime (không SDK) dù cài đủ; dùng `dotnet --list-runtimes` để detect đúng framework-dependent deploy
- [offline-deb-install-dpkg-version-mismatch-dependency.md](linux-desktop/offline-deb-install-dpkg-version-mismatch-dependency.md) — `dpkg -i` gói .deb offline fail nếu dependency (VD libcurl4) trên máy đích lệch version so với lúc tải; phải nhúng kèm đúng version dependency, cài cùng lúc

### python-tkinter/
- [python-lazy-import-startup-time.md](python-tkinter/python-lazy-import-startup-time.md) — App Tkinter khởi động 46s vì import ML ở mức module (`paddleocr` kéo torch GIÁN TIẾP); truy vết bằng hook `builtins.__import__`, fix bằng `find_spec` + proxy lazy + dựng tab lười → 2,1s
- [python-asyncio-shared-lock-concurrent-coroutines.md](python-tkinter/python-asyncio-shared-lock-concurrent-coroutines.md) — `asyncio.Lock()` tạo mới trong mỗi function = KHÔNG có mutual exclusion; background scheduler + HTTP handler đụng cùng file phải inject cùng 1 Lock instance; deadlock analysis: kiểm tra A→B và B→A trước khi thêm lock
- [dashboard-session-idle-means-still-running.md](python-tkinter/dashboard-session-idle-means-still-running.md) — State "Idle" trong state machine session (5-30 phút không có event JSONL) KHÔNG đồng nghĩa agent đã done — agent đang chờ LLM inference cũng im lặng. Parent session cũng trở thành Idle khi đợi child chạy. Điều kiện `is_active` phải check `in ("Running","Idle")` cho cả parent lẫn child; fallback None (child chưa index) + parent sống → coi là active

### windows-tooling/
- [harness-scratchpad-overrides-project-temp-rule.md](windows-tooling/harness-scratchpad-overrides-project-temp-rule.md) — Gemini Agent harness tự bơm "Scratchpad Directory" vào system prompt mọi session, dễ đè lên rule global "bắt buộc temp/ trong project"; project có GEMINI.md không tự động chặn được — phải chủ động kiểm tra `Glob <project>/temp/` trước khi ghi file trung gian, kể cả từ MCP tool ngoài (Stitch/Figma)
- [bash-heredoc-collapses-backslash-in-python.md](windows-tooling/bash-heredoc-collapses-backslash-in-python.md) — Heredoc thu gọn `\`→`\` khiến Python đọc `\5` thành escape bát phân `\5`: replace không khớp, file nhiễm ký tự điều khiển, converter DOCX chết. Fix: dựng backslash bằng `chr(92)`
- [powershell-vietnamese-bom-parse-error.md](windows-tooling/powershell-vietnamese-bom-parse-error.md) — PS 5.1 đọc `.ps1` không BOM theo ANSI → chữ Việt gây lỗi parser sai hướng (`Missing '=' operator`)
- [virtualbox-chan-doan-vm-tu-host.md](windows-tooling/virtualbox-chan-doan-vm-tu-host.md) — VM "màn hình đen" sau mất điện thường chỉ là Plymouth splash, không hỏng: chẩn đoán từ host bằng `VBoxManage controlvm screenshotpng` + `guestproperty enumerate` TRƯỚC khi định chạy fsck
- [docx2pdf-false-failure-pdf-still-created.md](windows-tooling/docx2pdf-false-failure-pdf-still-created.md) — `docx2pdf` báo `Word.Application.Quit` / RPC failed nhưng PDF **vẫn được tạo đúng**: kiểm file trên đĩa trước khi kết luận thất bại, đừng retry hay cài LibreOffice
- [graphify-hook-silent-fail-scripts-not-in-path.md](windows-tooling/graphify-hook-silent-fail-scripts-not-in-path.md) — ⚠️ `graphify claude install` ghi PreToolUse hook nhưng vô hiệu IM LẶNG khi `Scripts/` không có trong PATH (Windows): `_resolve_graphify_exe()` suy từ `sys.executable` fail vì `python.exe` và `graphify.exe` khác thư mục; Gemini Agent không báo lỗi khi hook fail — tool gốc vẫn chạy như chưa cài gì. Fix: sửa `command` trong `.gemini/settings.json` trỏ đường dẫn tuyệt đối `Scripts/graphify.exe`
- [workflow-tool-args-must-be-json-not-string.md](windows-tooling/workflow-tool-args-must-be-json-not-string.md) — Tool `Workflow`: gõ JSON lồng sâu trực tiếp làm tham số `args` vẫn bị hệ thống serialize thành STRING (không tự parse) → script `args.en.packs` = undefined, fail ngay ~25ms 0 agent chạy. Fix: nhúng cấu hình thành `const CONFIG = {...}` literal ngay trong thân script thay vì dựa vào `args` cho object lồng sâu
- [cross-tool-sync-script-clobbers-hand-forked-variant.md](windows-tooling/cross-tool-sync-script-clobbers-hand-forked-variant.md) — Script đồng bộ Gemini↔Gemini copy verbatim theo path tương đối sẽ ghi đè mất GUI tool đã hand-fork riêng tên (`ClaudeConfigAudit.ps1` vs `GeminiConfigAudit.ps1`) nếu không loại trừ; "orphan" trong dry-run không đồng nghĩa "an toàn để xoá". Mở rộng: cơ chế permission thật của Antigravity nằm ở `~/.gemini/config/config.json` (`globalPermissionGrants.allow`, string phẳng), không phải `settings.json`; dấu `*` trong pattern không đảm bảo hoạt động như wildcard

### react-web/
- [tsc-noemit-vs-tsc-build-different-configs.md](react-web/tsc-noemit-vs-tsc-build-different-configs.md) — `tsc --noEmit` đọc tsconfig gốc (gần như không kiểm gì), `tsc -b` mới đọc `tsconfig.app.json`; kèm bẫy `erasableSyntaxOnly` cấm parameter property
- [percent-quota-relative-vs-absolute-share-of-total-pool.md](react-web/percent-quota-relative-vs-absolute-share-of-total-pool.md) — Chia % công việc cho nhiều user: `percent/totalPercent` (tương đối GIỮA các user đã cấu hình) cho kết quả SAI khi tổng % ≠ 100 hoặc chỉ 1 user (nhận trọn 100% pool dù percent=30) — đúng ngữ nghĩa yêu cầu là % TUYỆT ĐỐI trên tổng pool (`percent/100 * TOTAL_POOL`), phần dư không ai nhận là ĐÚNG ý "cho phép tự do"
- [react19-jsx-namespace-and-lazy-swap-swallows-keystrokes.md](react-web/react19-jsx-namespace-and-lazy-swap-swallows-keystrokes.md) — React 19 bỏ JSX namespace toàn cục (phải `declare module 'react'`); tráo component sau `import()` động lúc đang gõ làm **mất phím im lặng**
- [web-speech-synthesis-chain-hangs-missing-voice.md](react-web/web-speech-synthesis-chain-hangs-missing-voice.md) — Hàm mới thêm vào module IIFE (`window.U = (function(){...})()`) quên đưa vào `return {...}` → `U.fn()` là `undefined`, throw TypeError trong `.then()`, giết im lặng mọi bước TTS xếp sau trong promise chain (không lỗi hiển thị); kèm hardening phụ: timeout dự phòng trong `speak()` + `hasVoice()` guard trước khi đọc ngôn ngữ phụ (tránh giọng sai khi máy thiếu voice)
- [stale-promise-chain-interrupts-new-async-work.md](react-web/stale-promise-chain-interrupts-new-async-work.md) — Chuỗi `.then()` TTS của item CŨ chưa kết thúc vẫn tiếp tục gọi `speechSynthesis.speak()` (tự `cancel()` hàng đợi) sau khi item MỚI đã bắt đầu đọc → cắt ngang lẫn nhau, âm thanh lộn xộn; fix bằng generation token (`speechGen`/`alive()`) — mỗi chuỗi mới tăng token, các bước `.then()` sau của chuỗi cũ tự no-op khi token đã lỗi thời
- [stale-closure-window-listener-drag-wrong-element.md](react-web/stale-closure-window-listener-drag-wrong-element.md) — Canvas drag: `onMouseDown` vừa `setSelectedId(hit.id)` vừa `attachWindowDragListeners()` ngay sau — closure `handleDragMove` đóng theo `selectedId` CŨ (render trước), kéo box A (chưa chọn từ trước) lại di chuyển nhầm box B (đang chọn). Fix: dùng `dragRef.current.orig.id` (ref, luôn mới) thay `selectedId` (state) trong logic bên trong window listener
- [flex-input-minwidth-auto-overflows-sidebar-row.md](react-web/flex-input-minwidth-auto-overflows-sidebar-row.md) — `<input>` trong hàng flex hẹp có `flex:1` nhưng vẫn giữ `min-width:auto` mặc định (~170-190px, KHÔNG bị flex:1 ghi đè) → ép cả hàng tràn, đẩy nút xoá ✕ ra NGOÀI vùng nhìn thấy (đo được x nằm ngoài container) + bóp ô màu 22px xuống 6px. Fix: `min-width: 0` trên input + `flex-shrink: 0` rõ ràng cho control cố định kích thước
- [react-onwheel-passive-preventdefault-silently-ignored.md](react-web/react-onwheel-passive-preventdefault-silently-ignored.md) — JSX `onWheel={handler}` bị React gắn thành passive listener mặc định → `e.preventDefault()` bị trình duyệt lặng lẽ bỏ qua (chỉ warning), scroll native VẪN chạy song song với zoom tự tính → giật/lệch. Fix: `addEventListener('wheel', handler, {passive:false})` trong `useEffect` — kèm bẫy deps=[] chạy trước khi containerRef gắn DOM thật nếu component có early-return
- [flex-justify-center-clips-unreachable-overflow.md](react-web/flex-justify-center-clips-unreachable-overflow.md) — Container `overflow:auto` + `display:flex` + `justify-content/align-items: center` (không "safe"): khi con (canvas zoom) lớn hơn container, trình duyệt tràn ĐỀU 2 phía nhưng CHỈ cho scroll tới phần tràn cuối — phần tràn ĐẦU (trái/trên) bị cắt vĩnh viễn không kéo tới được, chênh lệch TĂNG DẦN theo zoom. Fix: đổi `center` → `safe center` (CSS Box Alignment L3)
- [undo-stack-pollution-from-noop-click-select.md](react-web/undo-stack-pollution-from-noop-click-select.md) — Click-chọn 1 box đã có (không kéo đi đâu) vẫn push 1 snapshot "rỗng" vào undo stack (giống hệt trạng thái hiện tại) — Ctrl+Z phải bấm nhiều lần mới thấy tác dụng thật, cảm giác "lúc được lúc không". Fix: so sánh `JSON.stringify` trước/sau drag, chỉ push nếu THỰC SỰ thay đổi
- [vite-dev-server-localhost-only-lan-unreachable.md](react-web/vite-dev-server-localhost-only-lan-unreachable.md) — Máy khác trong LAN không truy cập được Vite dev server dù đã mở firewall — firewall không đủ, Vite mặc định CHỈ bind `localhost`, không lắng nghe interface LAN. Fix: `server.host: true` trong `vite.config.ts` (tự in dòng "Network:" kèm IP) + CORS backend cho phép origin dải mạng riêng (192.168.x/10.x/172.16-31.x) khi `NODE_ENV !== 'production'`
- [keyboard-hotkey-input-allows-value-that-global-handler-never-reaches.md](react-web/keyboard-hotkey-input-allows-value-that-global-handler-never-reaches.md) — Ô nhập "phím tắt tuỳ chỉnh" cho phép gõ số 1-9 làm hotkey, nhưng global keydown handler đã `return` sớm ở nhánh "phím 1-9 = chọn nhãn theo MRU" TRƯỚC khi tới đoạn so khớp custom hotkey → hotkey số lưu thành công nhưng vĩnh viễn không phím nào kích hoạt được (im lặng cả 2 đầu). Fix: chặn tại input (`alert` + revert) + chặn tại server (`normalizeHotkey` reject số)
- [percent-quota-relative-vs-absolute-share-of-total-pool.md](react-web/percent-quota-relative-vs-absolute-share-of-total-pool.md) — Tính năng chia % công việc cho nhiều user: công thức đầu `percent / sum(percent đã cấu hình)` khiến 1 user DUY NHẤT có percent bất kỳ (dù 30%) luôn nhận HẾT 100% pool — vì % là tỉ lệ TƯƠNG ĐỐI giữa những người cấu hình, không phải % TUYỆT ĐỐI trên tổng. Tự phát hiện bằng 1 test case "chỉ 1 user có %" trước khi ship. Fix: `target = round(percent/100 * TOTAL)`, `need = max(0, target - đã_có)` — chỉ top-up phần thiếu
- [js-date-parse-python-microseconds-nan.md](react-web/js-date-parse-python-microseconds-nan.md) — `new Date("2026-08-06T08:30:00.123456+00:00")` trả NaN: Python `datetime.isoformat()` sinh 6 chữ số microseconds, ECMAScript Date chỉ hỗ trợ 3 chữ số (milliseconds). Fix: `normalizeIso()` truncate `.xxxxxx` → `.xxx` + đổi `+00:00` → `Z` trước khi parse; thêm fallback "dd/MM HH:mm" khi diff > 24h

### csharp-winforms/
- [winforms-modal-child-hidden-behind-dialoghost.md](csharp-winforms/winforms-modal-child-hidden-behind-dialoghost.md) — Popup mới đè lên dialog xác nhận (`dialogHost` borderless Form riêng) bị che khuất im lặng nếu tự viết `Form.ShowDialog(owner)`; pattern đúng trong codebase là UserControl con + `MaskedUserControl` (như `UcSelectVouchers`), không phải Form riêng + TopMost
- [thread-async-void-workerthread-pollingstop-vo-hieu.md](csharp-winforms/thread-async-void-workerthread-pollingstop-vo-hieu.md) — ⚠️ CRITICAL: `new Thread(async void WorkerThread)` → thread chết ngay tại `await` đầu tiên, `Running`/`PollingStop()` thành vô hiệu, mỗi lần mở/đóng màn Cài đặt lại nhân đôi số vòng polling UDP tới thiết bị; fix bằng `Task` + `CancellationTokenSource`, `PollingStop` phải await vòng cũ thoát thật
- [winforms-designer-programmatic-ui.md](csharp-winforms/winforms-designer-programmatic-ui.md) — Programmatic UI không render trong VS Designer: dùng `*.UI.cs` + `[DesignerCategory("Code")]`
- [kzbutton-focus-events-override-custom-colors.md](csharp-winforms/kzbutton-focus-events-override-custom-colors.md) — `KzButton` (Kztek.Control8) tự reset FillColor/BorderColor/ForeColor khi Focus đổi — set màu custom phải subscribe GotFocus/LostFocus SAU để ghi đè lại
- [winforms-usercontrol-anchor-right-invisible-in-flowlayoutpanel.md](csharp-winforms/winforms-usercontrol-anchor-right-invisible-in-flowlayoutpanel.md) — UserControl set `Anchor` Right trên chính nó → co về ~0 kích thước khi add runtime vào FlowLayoutPanel, item "biến mất" không exception
- [winforms-flowlayoutpanel-clientsize-zero-after-modal.md](csharp-winforms/winforms-flowlayoutpanel-clientsize-zero-after-modal.md) — `FlowLayoutPanel.ClientSize.Width` trả 0 ngay sau `ShowDialog()` đóng — layout chưa recalculate; fix bằng `BeginInvoke(UpdateControlWidths)` + `Math.Max(MIN, ...)`
- [winforms-suspendlayout-unbalanced-anchor-not-recalculated.md](csharp-winforms/winforms-suspendlayout-unbalanced-anchor-not-recalculated.md) — Gõ `SuspendLayout()` vào chỗ đáng ra là `ResumeLayout()` → suspend count +2 mỗi lần mở dialog, layout engine treo vĩnh viễn: `Parent.Size == Child.Size` vẫn khớp nhưng `Anchor` của control BÊN TRONG ngừng tính lại → nút X + hàng nút bị cắt, im lặng không exception

### avalonia/
- [autofill-dropdown-picks-wrong-existing-entry.md](avalonia/autofill-dropdown-picks-wrong-existing-entry.md) — Auto-select "best match" dropdown chỉ lọc `ExistsOnSystem`, quên lọc `IsRecommended` → tự chọn nhầm app hệ thống (VD "Software & Updates") khi không còn app đích thật trên máy
- [kzkeyboard-enter-inserts-newline-ignores-acceptsreturn.md](avalonia/kzkeyboard-enter-inserts-newline-ignores-acceptsreturn.md) — `KzKeyboard` phím ENTER gọi thẳng `TypeTextRaw("\n")` chèn text bằng code, bỏ qua hoàn toàn `TextBox.AcceptsReturn` (thuộc tính này chỉ chặn Enter vật lý qua `OnKeyDown`); fix: check `TargetTextBox?.AcceptsReturn == true` trước khi chèn "\n"
- [kzkeyboard-telex-uye-cluster-and-language-label-swapped.md](avalonia/kzkeyboard-telex-uye-cluster-and-language-label-swapped.md) — `KzKeyboard`: label nút toggle ngôn ngữ bị đảo ngược (hiện "ENG" khi Telex đang BẬT thay vì "VIE"); `KzTelexEngine.FindTonePos` đặt sai dấu thanh cho cụm "uyê" (nguyễn/chuyện/khuyến ra "Nguỹên" thay vì "Nguyễn") vì áp nhầm quy tắc "3 nguyên âm → giữa cụm"
- [kzkeyboard-telexenabled-property-disconnected-from-toggle.md](avalonia/kzkeyboard-telexenabled-property-disconnected-from-toggle.md) — `KzKeyboard`: phím TELEX bấm thật chỉ đổi field private `_telexEnabled`, KHÔNG đụng `TelexEnabledProperty` (StyledProperty public) → host app subscribe `PropertyChanged` để lưu/persist ngôn ngữ KHÔNG BAO GIỜ fire dù UI vẫn đổi đúng bằng mắt; fix: xoá field private, dùng property public làm nguồn duy nhất kể cả trong rendering/xử lý phím nội bộ
- [avalonia-visualroot-toplevelhost-not-window.md](avalonia/avalonia-visualroot-toplevelhost-not-window.md) — Avalonia 12.1: window drawn decorations → `VisualRoot` của control con là `TopLevelHost` KHÔNG phải `Window` → guard/cast `VisualRoot is/as Window` fail IM LẶNG (dialog không mở, không exception); dùng `TopLevel.GetTopLevel(this)`; bẫy kèm: `_ = ShowDialog(...)` vứt Task nên fault không tới global handler — phải await + try/catch + log guard fail
- [avalonia-itemscollection-clear-fires-selectionchanged-minus-one.md](avalonia/avalonia-itemscollection-clear-fires-selectionchanged-minus-one.md) — `ListBox.Items.Clear()` bắn `SelectionChanged(-1)` đồng bộ ngay trong Clear → wrapper control (KzSidebar) forward ra consumer gây crash ngầm lúc attach; fix: wrapper giữ+khôi phục SelectedIndex quanh rebuild, consumer guard index âm, set selection mặc định trong `Window.Opened` (không phải ctor)
- [avalonia-style-animation-rotatetransform-silent-not-running.md](avalonia/avalonia-style-animation-rotatetransform-silent-not-running.md) — Style Animation KeyFrame `RotateTransform.Angle` trên Arc KHÔNG chạy (silent, spinner đứng hình) trên Avalonia 12.1 — fix: xoay bằng `DispatcherTimer` code-behind 30fps; bẫy verify kèm: `Process.MainWindowHandle` trỏ window active gần nhất → script capture "loại trừ MainWindowHandle" bắt nhầm window
- [avalonia-findwindoww-not-matching-avalonia-window-title.md](avalonia/avalonia-findwindoww-not-matching-avalonia-window-title.md) — Win32 `FindWindowW(null, title)` trả 0 với window Avalonia CẢ top-level MainWindow dù title khớp từng codepoint (mở rộng G005) → tìm hwnd bằng `EnumWindows` + `GetWindowTextW(CharSet.Unicode)` so sánh thủ công, rồi `AutomationElement.FromHandle`
- [avalonia-uia-owned-dialog-not-desktop-child-async-void-invoke-swallow.md](avalonia/avalonia-uia-owned-dialog-not-desktop-child-async-void-invoke-swallow.md) — Verify UIAutomation: dialog `ShowDialog` Avalonia (owned window) KHÔNG xuất hiện trong `TreeScope.Children` của desktop → tìm hwnd bằng `EnumWindows` + `FromHandle`; exception trong async void Click handler bị `InvokePattern.Invoke` nuốt im lặng → debug bằng try/catch ghi ra label UI
- [avalonia-shutdownrequested-sync-wait-ihost-stopasync-deadlock.md](avalonia/avalonia-shutdownrequested-sync-wait-ihost-stopasync-deadlock.md) — Sync-block `IHost.StopAsync().GetResult()` trong `ShutdownRequested` → deadlock UI thread, app không thoát; fix: cancel-first (`e.Cancel=true` → stop trên threadpool + timeout → `Shutdown()` lại với flag chống re-entry); bẫy kèm: process treo lock exe → test sau chạy binary cũ
- [avalonia-incremental-build-skips-xaml-compile-false-zero-error.md](avalonia/avalonia-incremental-build-skips-xaml-compile-false-zero-error.md) — ⚠️ HIGH: `dotnet build --no-dependencies` (build tăng dần) BỎ QUA hẳn `CompileAvaloniaXaml` → báo `0 Error(s)` dù AXAML lỗi `AVLN2100`/`AVLN2000`; dấu hiệu nhận biết: số lỗi TỤT VỀ 0 khi chạy lại y nguyên lệnh (không phải đã sửa). Chỉ tin "0 Error" khi `rm -rf obj bin` + build ĐẦY ĐỦ `-m:1`; sau khi xoá obj thì KHÔNG dùng `--no-dependencies` (gây `CS0006 Metadata file ... ref/*.dll`)
- [avalonia-parallel-build-file-lock.md](avalonia/avalonia-parallel-build-file-lock.md) — Build song song solution Avalonia nhiều project gây `AVLN9999`/`CS0006` file lock ngẫu nhiên; fix: luôn build/test với `-m:1`
- [avalonia-12-watermark-systemdecorations-obsolete.md](avalonia/avalonia-12-watermark-systemdecorations-obsolete.md) — Avalonia 12: `TextBox.Watermark`→`PlaceholderText`, `Window.SystemDecorations`→`WindowDecorations` (AVLN5001); bẫy: custom control có StyledProperty tên `Watermark` thì KHÔNG đổi tên API công khai
- [avalonia-treeview-inner-scrollviewer-horizontal-autoscroll.md](avalonia/avalonia-treeview-inner-scrollviewer-horizontal-autoscroll.md) — TreeView có ScrollViewer NỘI BỘ trong template mặc định: bọc ScrollViewer ngoài `Disabled` không chặn được auto-scroll ngang khi select (BringIntoView); phải set `ScrollViewer.HorizontalScrollBarVisibility="Disabled"` trên chính TreeView
- [migration-compile-remove-hides-unmigrated-code.md](avalonia/migration-compile-remove-hides-unmigrated-code.md) — `<Compile Remove>` che giấu module chưa migrate: build 0 errors nhưng camera/REST API/Kocom chết im lặng; checklist audit + cấm vá bằng no-op
- [avalonia-config-window-blocks-ui-thread-sql-timeout.md](avalonia/avalonia-config-window-blocks-ui-thread-sql-timeout.md) — Màn Cấu hình lag nhiều giây khi SQL/parking chưa kết nối: constructor gọi `Load()` đồng bộ đọc DB không timeout → tách JSON-load nhanh khỏi DB-load async (Task.Run + Dispatcher), thêm `Connect Timeout=3` vào connection string
- [avalonia-fire-and-forget-async-still-blocks-ui-thread.md](avalonia/avalonia-fire-and-forget-async-still-blocks-ui-thread.md) — `_ = AsyncMethod()` fire-and-forget từ UI thread KHÔNG tự chạy nền: continuation sau mỗi `await` (không ConfigureAwait(false)/Task.Run) vẫn marshal về UI thread, ghi log SQLite đồng bộ trong loop nhiều trang → app đơ; fix bọc `Task.Run` + `Dispatcher.UIThread.Post` khi update property
- [avalonia-clearvalue-removes-xaml-binding.md](avalonia/avalonia-clearvalue-removes-xaml-binding.md) — `Border.ClearValue(BorderBrushProperty)` trong code-behind xóa luôn binding XAML gốc → border biến mất ở trạng thái default; set brush tường minh thay vì ClearValue
- [avalonia-canvas-pathfinding-winforms-port.md](avalonia/avalonia-canvas-pathfinding-winforms-port.md) — Port pathfinding WinForms→Avalonia: dùng foot-on-edge (nearest edge) thay nearest-vertex; polyline liên tục entity→foot→vertices→foot→entity
- [avalonia-migration-review-checklist.md](avalonia/avalonia-migration-review-checklist.md) — Checklist 18 issues điển hình bị bỏ sót khi migrate WinForms→Avalonia: SQL injection, System.Drawing, race condition, memory leak, MVVM anti-patterns
- [avalonia-migration-from-winforms.md](avalonia/avalonia-migration-from-winforms.md) — Chuyển đổi WinForms → Avalonia: pitfall và pattern bắt buộc
- [avalonia-large-viewmodel-srp-extraction-pattern.md](avalonia/avalonia-large-viewmodel-srp-extraction-pattern.md) — ViewModel Avalonia phình to (God Object): tách từng trách nhiệm (filter, tracker, log buffer) thành class `sealed` riêng, VM compose + delegate — computed property trỏ instance con, giữ alias cho `const` cũ để không breaking binding
- [kztabcontrol-no-render-programmatic-items.md](avalonia/kztabcontrol-no-render-programmatic-items.md) — KzTabControl không render: workaround `<TabControl>` + fix ControlTheme BasedOn
- [avalonia-xmlns-using-vs-clr-namespace.md](avalonia/avalonia-xmlns-using-vs-clr-namespace.md) — Dùng `using:Namespace` thay `clr-namespace:` cho Avalonia built-in types — tránh AVLN2000
- [avalonia-property-subscription-before-visual-tree.md](avalonia/avalonia-property-subscription-before-visual-tree.md) — AvaloniaProperty OnPropertyChanged fires trước visual tree attach — không đặt subscription trong `_control is not null` guard
- [avalonia-onattachedtovisualtree-duplicate-event-wiring.md](avalonia/avalonia-onattachedtovisualtree-duplicate-event-wiring.md) — Nút toggle "bấm không có tác dụng": `OnAttachedToVisualTree` bị gọi lại nhiều lần → `btn.Click +=` cộng dồn handler → state bị đảo chẵn lần → fix bằng cờ `_eventsWired` chống gắn trùng
- [avalonia-back-button-only-icon-wired-not-label.md](avalonia/avalonia-back-button-only-icon-wired-not-label.md) — Nút "Quay lại" bấm vào chữ không có tác dụng: bản gốc WinForms wire Click cho CẢ Label lẫn PictureBox icon riêng, port chỉ wire icon → gộp icon+text vào 1 container, wire PointerPressed trên cả cụm; kích thước icon lấy từ `SetSize()` runtime, không lấy `.Designer.cs`/`.resx`
- [avalonia-find-actual-screen-before-editing.md](avalonia/avalonia-find-actual-screen-before-editing.md) — Xác định đúng màn hình/component trước khi sửa: component tên đúng nhưng chưa được dùng trong app → sửa sai chỗ
- [avalonia-datagrid-missing-fluent-styles.md](avalonia/avalonia-datagrid-missing-fluent-styles.md) — DataGrid render trắng hoàn toàn: FluentTheme KHÔNG tự include DataGrid styles — phải thêm `StyleInclude avares://Avalonia.Controls.DataGrid/Themes/Fluent.xaml` vào App.axaml
- [avalonia-ui-automation-screenshot-gotchas.md](avalonia/avalonia-ui-automation-screenshot-gotchas.md) — Automation chụp màn hình app Avalonia bằng Win32: Enter kích hoạt nút `IsDefault` (không phải nút đang focus) → dùng Tab-cuộn + click tọa độ; wheel event tổng hợp không cuộn ScrollViewer; đóng cửa sổ tin cậy nhất bằng `PostMessage WM_CLOSE` (không cần foreground); chụp dialog phải bám HWND riêng vì `MainWindowHandle` đổi khi cửa sổ con foreground
- [avalonia-winforms-port-gotchas.md](avalonia/avalonia-winforms-port-gotchas.md) — Gotcha build khi port WinForms→Avalonia: `[ObservableProperty]` trùng tên method (CS0102), `Theme` static đụng `Control.Theme`, ComboBox không có IsEditable (dùng AutoCompleteBox), custom Control không có Background, TreeDataTemplate cần DataType, SkiaSharp 2.88 DrawText API, SKBitmap NCHW/Rgba8888
- [avalonia-tabcontrol-lazy-visual-tree-columns.md](avalonia/avalonia-tabcontrol-lazy-visual-tree-columns.md) — TabControl lazy attach: tab chưa chọn có `InnerGrid = null` → columns không add → grid trắng dù footer đúng; fix bằng `SetColumns()` + `_pendingColumns` trong custom control
- [avalonia-namespace-collision-ipgs-kiosk.md](avalonia/avalonia-namespace-collision-ipgs-kiosk.md) — Project namespace `IPGS.Kiosk.Avalonia` che khuất `Avalonia.*` framework → CS0234; fix: `using Avalonia;` + unqualified type HOẶC `global::Avalonia.Type`
- [avalonia-writeablebitmap-binding-not-disposed-leak.md](avalonia/avalonia-writeablebitmap-binding-not-disposed-leak.md) — `WriteableBitmap` gán lại vào binding property `Image.Source` mỗi frame mà không dispose bản cũ → rò rỉ native pixel buffer (~120 MB/s ở 15fps 1080p); fix: gán mới rồi dispose bản cũ trên UI thread, thêm dispose cuối trong `Dispose()`
- [msbox-avalonia-namespace-split.md](avalonia/msbox-avalonia-namespace-split.md) — MsBox.Avalonia 3.0.0-rc2: `ButtonEnum` ở `MessageBox.Avalonia.Enums` (legacy ns), `Icon` ở `MsBox.Avalonia.Enums` (mới) — cần 2 using riêng; dùng overload 2 tham số để tránh phức tạp
- [msbox-avalonia-3rc2-incompatible-avalonia-12.md](avalonia/msbox-avalonia-3rc2-incompatible-avalonia-12.md) — ⚠️ MsBox.Avalonia 3.0.0-rc2 (bản duy nhất trên NuGet) biên dịch với Avalonia.Controls 11 → `TypeLoadException: Avalonia.Controls.SystemDecorations` ngay khi gọi `GetMessageBoxStandard` trên Avalonia 12, BUILD VẪN XANH. Không có bản nào hỗ trợ Avalonia 12 → gỡ gói, tự viết `MessageDialogWindow` (~110 dòng). Kèm pattern self-test qua biến môi trường để bắt lớp lỗi 'chỉ lộ lúc chạy'
- [kzpasswordtextbox-usercontrol-oneway-text-binding.md](avalonia/kzpasswordtextbox-usercontrol-oneway-text-binding.md) — `KzPasswordTextBox` (UserControl) bind `Text` mặc định OneWay → VM không nhận input, silent failure; PHẢI `Mode=TwoWay` + set `Watermark=""` (default "Nhập mật khẩu"); CornerRadius/FontSize set ngoài bị bỏ qua — kiểm tra `defaultBindingMode` mọi StyledProperty tự đăng ký của control Kz dạng UserControl
- [kz-component-wrap-workaround-pattern.md](avalonia/kz-component-wrap-workaround-pattern.md) — Wrap Kz Avalonia component (pre-built DLL không có source) khi thiếu API vs WinForms control gốc: probe API bằng MetadataLoadContext → phân loại A/B/C → 3-workaround W1 Grid+Image / W2 IsHitTestVisible / W3 scale trong VM (tiết kiệm 80% effort so build mới)
- [avalonia-dispatcher-invokeAsync-no-AsTask.md](avalonia/avalonia-dispatcher-invokeAsync-no-AsTask.md) — `Dispatcher.UIThread.InvokeAsync()` trả về `DispatcherOperation` không có `.AsTask()` (WPF pattern) — dùng `await` trực tiếp với `async Task` method
- [avalonia-attachdevtools-double-window.md](avalonia/avalonia-attachdevtools-double-window.md) — `AttachDevTools()` gọi SAU khi gán `desktop.MainWindow` → window đầu tiên bị attach 2 lần → F12 mở 2 bảng DevTools giống hệt nhau; luôn gọi `AttachDevTools()` TRƯỚC khi gán MainWindow
- [avalonia-config-json-property-name-mismatch-winforms-shared-file.md](avalonia/avalonia-config-json-property-name-mismatch-winforms-shared-file.md) — Config model port từ WinForms: field JSON đọc rỗng vì đặt tên property "đẹp" thay vì đúng tên gốc (`PGSDbServer`, `PasskingPassword`...) trong file config.txt DÙNG CHUNG với bản WinForms; enum field serialize dạng int ordinal phải giữ int trong model, quy đổi label ở ViewModel
- [avalonia-kzbutton-padding-swallows-narrow-glyph.md](avalonia/avalonia-kzbutton-padding-swallows-narrow-glyph.md) — `KzButton` Padding Thickness "H,V" áp H cho CẢ 2 bên (không chia đôi) → nút icon-only Width=40 + kz-size-md padding 16,0 = 32px ngang nuốt hết glyph "+/−" chỉ còn thấy dấu chấm; fix set `Padding="0"` local trên nút. Cùng gốc rễ gây cột DataGrid hẹp (Port/Kênh/ZCU) bị cắt chữ
- [avalonia-12-breaking-changes-rabbitmq7-migration.md](avalonia/avalonia-12-breaking-changes-rabbitmq7-migration.md) — Nâng Avalonia 11→12: xóa `GotFocusEventArgs` → dùng `Avalonia.Input.FocusChangedEventArgs` (KHÔNG phải RoutedEventArgs) cho cả GotFocus/LostFocus; `AncestorType=UserControl` hết resolve property con (dùng `x:Name`+`ElementName` thay thế); `TreeViewItem.ShowExpander` bị xóa hẳn; siết AVLN2100 (bắt buộc `x:DataType`, kể cả trong `ControlTheme`). Nâng RabbitMQ.Client 6→7: rewrite async-only (`IModel`→`IChannel`, `*Async` suffix, `IAsyncDisposable`) — bridge sync/await 1 điểm bằng `ConfigureAwait(false)+GetAwaiter().GetResult()`, không dùng `lock` quanh code có await (đổi SemaphoreSlim)
- [avalonia-migration-silent-behavior-drop-loading-flow.md](avalonia/avalonia-migration-silent-behavior-drop-loading-flow.md) — Port loading flow WinForms→Avalonia dễ rớt nhánh lỗi/retry ngầm định (nhánh "computer chưa đăng ký" bị bỏ, không lỗi build nhưng sai hành vi); port đệ quy retry của WinForms phải đổi thành `while` loop tránh stack overflow
- [avalonia-countdown-reset-on-click-missing-targetcontrol-hook.md](avalonia/avalonia-countdown-reset-on-click-missing-targetcontrol-hook.md) — Countdown auto-return không reset khi click: bản gốc `KzRoundCountdown.TargetControl` hook MouseDown đệ quy lên Form+mọi control con, port Avalonia chỉ copy DispatcherTimer đếm lùi mà bỏ sót cơ chế reset-on-interaction; fix bằng interface `IResettableCountdown` + 1 handler PointerPressed Tunnel duy nhất ở MainWindow
- [avalonia-nested-path-binding-no-refresh-same-reference.md](avalonia/avalonia-nested-path-binding-no-refresh-same-reference.md) — Binding lồng `"Vm.Model.Field"` với Model non-INPC: raise PropertyChanged trên property CHA (VD "Model") không lan refresh xuống field con nếu reference object không đổi — TextBox im lặng không cập nhật dù giá trị đã đổi đúng; fix bằng proxy property trên ViewModel bind trực tiếp
- [avalonia-border-child-not-clipped-not-stretched.md](avalonia/avalonia-border-child-not-clipped-not-stretched.md) — `Border` có `CornerRadius` KHÔNG tự clip nội dung con theo góc bo (phải set `ClipToBounds="True"` tường minh); Control gán động vào `Border.Child` bằng code-behind không tự Stretch — phải set `HorizontalAlignment`/`VerticalAlignment=Stretch` tường minh trước khi gán. Case: camera live preview tràn border, video không lấp đầy khung
- [avalonia-minwidth-minheight-hard-constraint-not-default-size.md](avalonia/avalonia-minwidth-minheight-hard-constraint-not-default-size.md) — ⚠️ CRITICAL: Port WinForms `Width=640;Height=480;` (kích thước khởi tạo) → Avalonia `MinWidth/MinHeight` (RÀNG BUỘC CỨNG) sai ngữ nghĩa, khiến control video luôn ≥640×480 dù panel cha nhỏ hơn — tràn border (không ClipToBounds) hoặc crop/zoom hẹp (có ClipToBounds), cùng 1 gốc 2 triệu chứng khác nhau
- [avalonia-attachedtovisualtree-zero-bounds-camera-motion.md](avalonia/avalonia-attachedtovisualtree-zero-bounds-camera-motion.md) — ⚠️ CRITICAL: `AttachedToVisualTree` fires trước Measure/Arrange → `Bounds.Width=0` → camera decode 1920×1080 thay vì ~300px; kết hợp với Motion detection → CPU spike → "Not Responding". Fix: `DispatcherPriority.Loaded`
- [avalonia-migration-theme-drift-not-caught-by-build-pass.md](avalonia/avalonia-migration-theme-drift-not-caught-by-build-pass.md) — ⚠️ CRITICAL: migrate WinForms→Avalonia, agent tự ý đổi theme (nền trắng gốc → navy/brand KZTEK, mất màu đỏ cảnh báo, mất hover-swap-image, ASCII thay tiếng Việt có dấu) mà "build PASS 0 error" không phát hiện được — PHẢI đọc `.Designer.cs` gốc (không chỉ `.cs`) đối chiếu màu/ảnh/vị trí, và audit visual RIÊNG sau mỗi nhóm task migrate, không tin build PASS làm tiêu chí duy nhất
- [avalonia-navigation-switch-missing-entry-routes.md](avalonia/avalonia-navigation-switch-missing-entry-routes.md) — ⚠️ CRITICAL: nhiều task migrate song song mỗi task tự thêm case cho màn hình ĐÍCH của mình nhưng bỏ sót case cho route DẪN VÀO (entry point như `CarBranch`/`MotorBranch`/route gọi bằng string literal) — bấm nút không chuyển màn hình, không lỗi không crash vì `default:` chỉ log; fix bằng cách đối chiếu toàn bộ `AppRoutes` constants + `NavigateTo("...")` string literal với toàn bộ case đã implement sau khi các task song song hoàn thành
- [avalonia-port-must-read-designer-backcolor-not-assume-theme.md](avalonia/avalonia-port-must-read-designer-backcolor-not-assume-theme.md) — Bảng màu gốc kiosk cụ thể (lblTime #9EA1A2, value #2A2F30/#251C53, nút phụ ControlLight #E3E3E3+Black, Invoice #251C53, CTA #F05922, còn-lại đỏ #DA291C) + gotcha KzKeyboard≠ucSearchNumber (dialpad tròn → build control riêng, AddHandler Button.ClickEvent Bubble ở root) + lỗi port dở AVLN2000 thiếu .axaml.cs / BoolConverters.False không tồn tại
- [avalonia-systemutils-logger-null-multyplatform.md](avalonia/avalonia-systemutils-logger-null-multyplatform.md) — `IPGS.Kiosk.Avalonia` crash NullReferenceException tại `SystemUtils.logger.SaveAPILogDetail(...)`: `Program.cs`/`App.axaml.cs` (entry point mới) quên copy dòng init `SystemUtils.logger = LoggerFactory.CreateLoggerService(...)` từ `IPGS.Kiosk/Program.cs` gốc; project Avalonia reference `Kztek.Tool.MultyPlatform` — `LoggerFactory` nằm namespace RIÊNG `Kztek.Tool.LogHelpers`, không giống bản WinForms
- [avalonia-devtools-package-migration-v12.md](avalonia/avalonia-devtools-package-migration-v12.md) — Core lên Avalonia 12.1.0: package `Avalonia.Diagnostics` KHÔNG có bản 12.x (dừng ở 11.3.18) → phải đổi hẳn sang `AvaloniaUI.DiagnosticsSupport` (versioning riêng, hiện 2.2.3), API đổi `this.AttachDevTools()` (trên `Application`) → `.WithDeveloperTools()` (trên `AppBuilder` trong `Program.cs`); project tham chiếu khác trong tổ chức mới chỉ khai báo package, CHƯA wire — không suy luận "có package = đã dùng được"
- [avalonia-navigation-null-api-response-wrong-route.md](avalonia/avalonia-navigation-null-api-response-wrong-route.md) — NavigateTo sai route khi API trả null: `remain=0` (TotalAmount=null-coalesced 0) bị hiểu nhầm là "đã trả đủ" → điều hướng sang `BikePaymentResult` thay vì `MotorInforDetail`; fix bằng `detail?.IsPaid` thay vì tính remain từ DTO null-coalesced
- [avalonia-goback-history-reenters-autonaviate-waiting-screen.md](avalonia/avalonia-goback-history-reenters-autonaviate-waiting-screen.md) — `_nav.GoBack()` (pop history chung) trên `MotoQrImageView` quay lại `MotoWaitingQrView` — screen này tự `NavigateTo` tiếp khi attach (tạo giao dịch QR mới) nên back "trông như vô tác dụng"; fix bằng event `BackRequested` navigate thẳng về `MotorBranch`, không qua history stack
- [avalonia-avaloniaresource-not-copied-to-output.md](avalonia/avalonia-avaloniaresource-not-copied-to-output.md) — ⚠️ HIGH: `<AvaloniaResource Include="Assets\**">` chỉ nhúng file vào assembly (`avares://`), KHÔNG copy vật lý ra `bin/` → `SoundServiceImpl`/`SoundServiceLinux`/`PrinterServiceImpl` đọc bằng physical path (`AppContext.BaseDirectory`+`Path.Combine`) fail âm thầm (catch-log-only) — chuyển màn không nghe âm thanh, build vẫn PASS 0 Error; fix thêm `<Content Include="Assets\Sound\**;Assets\Templates\**"><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory></Content>` song song
- [avalonia-canvas-direct-child-horizontalalignment-ignored.md](avalonia/avalonia-canvas-direct-child-horizontalalignment-ignored.md) — ⚠️ HIGH: `Canvas` bỏ qua `HorizontalAlignment`/`VerticalAlignment` của con TRỰC TIẾP (Arrange rect luôn khít DesiredSize) — `StackPanel`/`Grid` con Canvas dù set `Width="1080" HorizontalAlignment="Center"` vẫn lệch trái; fix bằng bọc thêm 1 Grid trung gian, dùng cột `ColumnDefinitions` pixel cố định (không `Auto`) để icon ẩn/hiện không làm nội dung khác dịch chuyển
- [avalonia-itemscontrol-canvas-position-compiled-binding.md](avalonia/avalonia-itemscontrol-canvas-position-compiled-binding.md) — `ItemsControl` + `ItemsPanel=Canvas`: đặt `Canvas.Left/Top` trong `ItemTemplate` build sạch nhưng mọi item đè nhau tại (0,0) (con trực tiếp của Canvas là `ContentPresenter`, không phải gốc template); đặt qua `Style` nhắm container thì compiled binding resolve theo `x:DataType` của View → `AVLN2000 Unable to resolve property 'X'`. Fix: `ItemsPanel=Grid` + `Margin="{Binding Offset}"` (`Thickness(X,Y,0,0)`) + bắt buộc `HorizontalAlignment=Left`/`VerticalAlignment=Top`
- [avalonia-appstate-logopath-never-wired.md](avalonia/avalonia-appstate-logopath-never-wired.md) — ⚠️ HIGH: `AppState.LogoPath/SubLogoPath/AdsPath` có comment "set từ KioskAppConfig khi App khởi động" nhưng `App.axaml.cs` KHÔNG BAO GIỜ gán 3 dòng này → logo trống trên MỌI view toàn app, không exception, build PASS; luôn grep `AppState\.\w+\s*=` để verify wiring thật, đừng tin comment
- [avalonia-fixed-size-clips-content-silently.md](avalonia/avalonia-fixed-size-clips-content-silently.md) — ⚠️ HIGH: kích thước cố định CẮT nội dung im lặng (không warning/exception, build 0 Error): (1) `TextBlock Height="32"` với `FontSize="28"` → cắt chữ trên/dưới vì hộp dòng font 28px cần ~38–40px, lỗi nhân bản ra 13 view do copy cùng khuôn; (2) root `Width="975"` mà nội dung cần `49+472+30+483+49=1083` → cắt mất 108px cả cột nút. Luật: `Height >= FontSize*1.4` hoặc dùng `MinHeight`; cộng tay tổng lề+cột+khe trước khi tin layout; đọc `Location` của control con trong Designer để lấy khe/lề THẬT, đừng suy từ `Margin` của control lồng trong
- [avalonia-1x1-placeholder-icon-asset-never-replaced.md](avalonia/avalonia-1x1-placeholder-icon-asset-never-replaced.md) — ⚠️ CRITICAL: icon "mất hoàn toàn" dù binding/build/rebuild+restart đều đúng — file `.png` thực chất là placeholder 1×1 alpha=0 sót lại từ migration (build không phát hiện được, file hợp lệ định dạng); verify bằng Pillow `im.size`/`getextrema()`, trích ảnh thật từ `.resx` gốc (`<PictureBoxName>.Image` base64) khớp control qua `.Designer.cs`, rồi quét toàn Assets tìm placeholder khác cùng đợt (tìm thấy 2/2 lần quét)
- [avalonia-application-restart-missing-after-config-save.md](avalonia/avalonia-application-restart-missing-after-config-save.md) — ⚠️ HIGH: Avalonia không có `Application.Restart()`; port chỉ gọi `Environment.Exit(0)` kèm giả định sai "OS/supervisor sẽ tự mở lại" → app tắt và đứng yên sau khi lưu config. Fix: `Process.Start(Environment.ProcessPath)` trước `Environment.Exit(0)`
- [avalonia-dialog-overlay-route-does-not-detach-underlying-view-timer-leak.md](avalonia/avalonia-dialog-overlay-route-does-not-detach-underlying-view-timer-leak.md) — Route dialog-overlay (`MotoPaymentComplete`) không swap MainContent → `DetachedFromVisualTree` của view phía sau không fire → countdown/DispatcherTimer riêng của view (VD `MotoVisaTransactionIdView`) vẫn chạy ngầm sau khi thanh toán xong; fix dừng tường minh qua `vm.PropertyChanged` khi `IsPaymentSuccess=true`, không dựa vào lifecycle detach
- [avalonia-navigation-recreated-vm-reconnects-infra-every-visit.md](avalonia/avalonia-navigation-recreated-vm-reconnects-infra-every-visit.md) — ⚠️ HIGH: `NavigateTo("Main")` tạo `new MainViewModel(...)` mỗi lần + `OnDetachedFromVisualTree` teardown Socket Server/RabbitMQ/Controller mỗi khi rời màn → mỗi lần quay lại Main đều reconnect lại toàn bộ hạ tầng; fix bằng cache ViewModel singleton ở NavigationService + guard `IsLoaded` + bỏ teardown khỏi lifecycle detach thường (chỉ teardown thật trước khi restart process)
- [avalonia-cash-acceptor-reused-cashresult-stale-flag.md](avalonia/avalonia-cash-acceptor-reused-cashresult-stale-flag.md) — ⚠️ CRITICAL: Driver CBA9/SSP (cũ lẫn `CBA9ControllersV3` mới) reuse CHỈ 1 object `CashResult` xuyên suốt phiên poll, `IsValidMoney`/`IsRejected` chỉ set `true` không bao giờ tự reset — bản gốc consumer tự `ClearCash(cash)` sau mỗi tờ tiền, migrate quên port bước này → tờ tiền đầu tiên nhận event bình thường, tờ thứ 2 trở đi im lặng hoàn toàn vì cờ kẹt `true` mãi mãi; fix bằng tự reset `cash.IsValidMoney=false; cash.MoneyValue=0` (và `IsRejected`) ngay sau khi raise event trong `CashDeviceServiceImpl.OnCba9PollAsync`
- [avalonia-shared-back-button-idisposable-cast-dead-code.md](avalonia/avalonia-shared-back-button-idisposable-cast-dead-code.md) — ⚠️ CRITICAL: `PaymentInfoViewModel.GoBack()` (nút Back dùng chung header cho Cash/QR/Visa/Voucher) cast `ActivePaymentContent is IDisposable disposable → disposable.Dispose()` nhưng KHÔNG sub-ViewModel nào implement `IDisposable` — dead code từ đầu, không lộ qua build/happy-path test; bấm Back giữa lúc thanh toán không unsubscribe CashDevice / không hủy `CancellationTokenSource` polling QR-Visa, khiến `Paid` không cập nhật đúng hoặc callback bắn trễ sau khi rời màn; fix bằng implement `IDisposable` thật trên `CarCashViewModel`/`CarQrViewModel`/`CarVisaViewModel`
- [avalonia-payment-partial-amount-reset.md](avalonia/avalonia-payment-partial-amount-reset.md) — ⚠️ HIGH: thanh toán một phần bằng cash/voucher (ô tô lẫn xe máy) bị mất khi quay lại màn chọn phương thức — DTO dựng lại bỏ qua `detail.Amount` từ API (ô tô), và `EventHandler?` không tham số ở MotoCash/MotoVoucher khiến `MainWindow` dùng lại DTO closure gốc (stale) thay vì state đã tích lũy; fix thêm `paidAmount` vào constructor DTO + đổi `EventHandler<long>?` mang theo state cập nhật
- [avalonia-cash-device-disconnect-shared-singleton-grey-icon.md](avalonia/avalonia-cash-device-disconnect-shared-singleton-grey-icon.md) — ⚠️ HIGH: `CleanUp()` gọi `CashDevice.Disconnect()` → `_isConnected=false` → icon Cash ở MainView xám vĩnh viễn sau giao dịch; MainViewModel singleton không gọi lại Connect(); fix: chỉ unsubscribe event, KHÔNG gọi Disconnect() trên shared singleton service
- [avalonia-cash-acceptor-silently-reenabled-on-reconnect.md](avalonia/avalonia-cash-acceptor-silently-reenabled-on-reconnect.md) — ⚠️ HIGH: `DisableAcceptor()` "không có tác dụng" ngắt quãng — KHÔNG do pattern `_ = ` fire-and-forget (queue FIFO vẫn giữ đúng thứ tự), mà do `TryConnectCore()` (CBA9ControllersV3) tự `EnableValidatorCore()` ở MỌI lần (re)connect kể cả auto-reconnect ngầm sau rớt kết nối thoáng qua, âm thầm xoá trạng thái disabled; fix bằng lưu ý định `_acceptorShouldBeEnabled` + tái áp đặt trong `OnConnectionStatus(isConnected=true)`
- [avalonia-config-tool-crash-on-corrupted-password.md](avalonia/avalonia-config-tool-crash-on-corrupted-password.md) — ⚠️ HIGH: tool `ApplicationConfig` (sửa config SQL/LPR/Parking) tự crash khi mở nếu `sql.txt` có Password không phải Base64 hợp lệ — `GetDecodePassword` gọi không try/catch trong `SqlConfigView.OnLoaded`, chặn luôn đường sửa config khi nó hỏng (chicken-egg); fix bọc try/catch riêng đoạn decode, fallback rỗng + cảnh báo UI
- [avalonia-loading-window-no-path-to-fix-config.md](avalonia/avalonia-loading-window-no-path-to-fix-config.md) — ⚠️ HIGH: `LoadingWindow` (app chính) báo "Không Kết Nối Được Đến SQL Server" chỉ có nút "Đóng", không có đường mở tool config — UX gap, không phải bug crash; fix thêm nút "Mở Cấu Hình" mở thẳng `ApplicationConfig.Views.MainWindow` trong CÙNG process (đã có ProjectReference sẵn) rồi tự retry loading sau khi đóng
- [avalonia-dispatchertimer-sync-sql-freezes-ui.md](avalonia/avalonia-dispatchertimer-sync-sql-freezes-ui.md) — ⚠️ CRITICAL: `DispatcherTimer.Tick` chạy TRÊN UI thread — handler gọi thẳng `SqlConnection.Open()` + DELETE đồng bộ (dọn log 10 giây/lần) làm UI đóng băng từng đợt: bấm nút `thất thường` lúc ăn lúc không, kéo-để-cuộn không ăn, Windows báo `is not responding`, nhưng đồng hồ trên UI vẫn nhảy nên dễ tưởng UI còn khoẻ. Dễ chẩn đoán sai sang `vùng chạm quá nhỏ`/`lớp phủ chặn hit-test`/`UIPI chặn input` (`SendInput` trả sent=3 KHÔNG nghĩa là app xử lý). Chỉ đích danh bằng `dotnet-stack report` lúc đang treo; fix bằng `Task.Run` + `Dispatcher.UIThread.Post` khi Start() lại timer. Kèm phụ lục điều khiển UI Avalonia bằng UI Automation để kiểm chứng
- [avalonia-onscreen-keyboard-overlay-blocks-scroll.md](avalonia/avalonia-onscreen-keyboard-overlay-blocks-scroll.md) — ⚠️ HIGH: bàn phím ảo đặt kiểu LỚP PHỦ đáy (`Grid.RowSpan` + `VerticalAlignment=Bottom`) che nội dung nhưng KHÔNG chiếm chỗ → `ScrollViewer.Viewport` không co lại nên phần form dưới bàn phím KHÔNG BAO GIỜ cuộn tới được (user báo `đã cuộn hết cỡ` mà vẫn bị che) và thanh nút Lưu/Huỷ bị che luôn; fix cho bàn phím một HÀNG RIÊNG trong Grid. Kèm 2 bẫy: `Padding` của ScrollViewer thu nhỏ viewport chứ KHÔNG nới Extent (phải đặt `Margin` trên content mới cuộn qua được trường cuối), và `BringIntoView()` gọi đồng bộ ngay sau `IsVisible=true` sẽ tính theo viewport CŨ → phải `Dispatcher.UIThread.Post(..., Background)`
- [avalonia-datepicker-selecteddate-datetimeoffset.md](avalonia/avalonia-datepicker-selecteddate-datetimeoffset.md) — ⚠️ HIGH: `DatePicker.SelectedDate` là `DateTimeOffset?`; bind vào `DateTime` → `InvalidCastException` hiện ĐỎ ngay trên control, ô ngày trống, **cả màn Báo cáo không dùng được** mà build vẫn sạch và không có gì trong log. Lỗi binding Avalonia hiện trên control chứ không ném exception → self-test "dựng được cửa sổ" KHÔNG chứng minh màn hình dùng được, phải mở nhìn từng màn. 3 control ngày giờ 3 kiểu khác nhau: DatePicker(`DateTimeOffset?`), TimePicker(`TimeSpan?`), CalendarDatePicker(`DateTime?`)
- [avalonia-global-textblock-style-breaks-button-content.md](avalonia/avalonia-global-textblock-style-breaks-button-content.md) — ⚠️ MEDIUM: `Style Selector="TextBlock"` toàn cục bắt CẢ TextBlock do ControlTemplate sinh ra bên trong `ContentPresenter` của Button → chữ nút nền navy bị đổi sang màu chữ tối, "Lưu"/"Đóng" đen trên đen. Selector trần cho control nguyên thuỷ (TextBlock/Border/Path) là vật liệu của mọi template — dùng selector có phạm vi (`Border.section-header > TextBlock`, class `.on-navy`) hoặc sửa `ControlTheme`. Dấu hiệu nhận biết: chỉ chữ trên nền ĐẬM sai màu, nền sáng vẫn đúng
- [avalonia-keysymbol-ctrl-modifier-control-char.md](avalonia/avalonia-keysymbol-ctrl-modifier-control-char.md) — ⚠️ HIGH: `KeyEventArgs.KeySymbol` khi giữ Ctrl trả về ASCII control-char (Ctrl+V=0x16) chứ KHÔNG phải chữ cái gốc — remote-control input-forwarding map thẳng KeySymbol thành keysym X11 khiến Ctrl+C/Ctrl+V không hoạt động; fix bằng skip KeySymbol tier khi `KeyModifiers.Control` đang giữ, dùng fallback theo `Key` enum. Kèm bug pointer event thiếu `e.Handled=true` khiến click "cả 2 bên cùng nhận"
- [avalonia-control-overwrites-xaml-property.md](avalonia/avalonia-control-overwrites-xaml-property.md) — `KzLabel.ApplyStyle()` gán thẳng `Foreground = ...` (LocalValue) trong `OnAttachedToVisualTree` → đè lên giá trị XAML mỗi lần re-attach, chữ tiêu đề tối trên nền navy; `IsSet()` không dò được vì `{DynamicResource}` chưa resolve xong. Fix: `SetValue(Property, value, BindingPriority.Style)` thay vì gán CLR trực tiếp
- [avalonia-kzbadge-onattachedtovisualtree-forces-neutral-class.md](avalonia/avalonia-kzbadge-onattachedtovisualtree-forces-neutral-class.md) — `KzBadge.OnAttachedToVisualTree` gọi `UpdateClasses()` vô điều kiện → xoá `Classes="kz-badge-pill kz-badge-success"` gán qua XAML, ép về mặc định `Variant=Neutral`/`BadgeType=StatusPill` mỗi lần attach; preview "mất màu" vì nền Neutral trùng màu nền Border bọc ngoài. Fix: chỉ gọi `UpdateClasses()` khi `IsSet(VariantProperty)||IsSet(BadgeTypeProperty)` — property enum thường nên `IsSet` dùng được (khác case Foreground/DynamicResource ở lesson trên). **Lặp lại ở `KzButton`** cùng ngày — nặng hơn vì thiếu cả `OnPropertyChanged` (Binding không đổi màu được)
- [avalonia-datagrid-auto-column-khong-do-lai.md](avalonia/avalonia-datagrid-auto-column-khong-do-lai.md) — `DataGridTemplateColumn Width="Auto"` chỉ đo bề rộng tại lần materialize đầu, KHÔNG đo lại khi thêm dòng mới nội dung dài hơn — dòng cũ hiện đủ, dòng mới bị cắt; `MinWidth` không cứu được. Fix: đặt `Width` cố định hoặc `*`, không dùng `Auto` cho cột nội dung thay đổi theo dòng
- [avalonia-icon-emoji-khong-doc-duoc-o-co-nho.md](avalonia/avalonia-icon-emoji-khong-doc-duoc-o-co-nho.md) — Icon emoji/pictograph nhiều chi tiết (bút chì `✏`, thùng rác `🗑`) render ĐÚNG nhưng nát ở cỡ toolbar 12-15px (không phải tofu thiếu font — phóng to 4-5x mới thấy hình thù thật); dùng hình nét đơn giản (`+`,`↺`,`✓`) cho UI nhỏ, bỏ icon phức tạp chỉ giữ nhãn chữ
- [avalonia-toast-child-window-hidden-on-hide.md](avalonia/avalonia-toast-child-window-hidden-on-hide.md) — `KzToastHost` là con của chính Window → `Window.Hide()` ngay sau `KzToast.Show()` làm toast biến mất trước khi user kịp đọc (minimize-to-tray); fix: `DispatcherTimer` one-shot delay ngắn hơn thời lượng toast rồi mới `Hide()`
- [avalonia-loading-progress-count-mismatch-vs-actual-usage.md](avalonia/avalonia-loading-progress-count-mismatch-vs-actual-usage.md) — ⚠️ HIGH: LoadingWindow báo "✔ 67 thiết bị" (đếm TỔNG `deviceResponse.Data.Count`) trong khi Main dùng `AppState.BdkControllers` (đã lọc theo Computer + loại `purpose==0`) rỗng → KPI 0/0 im lặng, không lỗi đỏ; progress-text phải đếm đúng biến sau lọc mà màn đích thực sự dùng, không phải response thô
- [avalonia-shared-handler-delegate-signature-mismatch-click-vs-eventhandler.md](avalonia/avalonia-shared-handler-delegate-signature-mismatch-click-vs-eventhandler.md) — Build AVLN3000 "Unable to find suitable setter/adder for property Click": dùng chung 1 method `OnBackRequested(object?, System.EventArgs)` cho cả `KioskHeaderBar.BackRequested` (`EventHandler`) và `KzButton.Click` (`EventHandler<RoutedEventArgs>`) — 2 event khác chữ ký delegate không tự cast; fix bằng wrapper `OnBackButtonClick(object?, RoutedEventArgs)` gọi lại handler chung
- [avalonia-observablecollection-read-off-uithread-race.md](avalonia/avalonia-observablecollection-read-off-uithread-race.md) — ⚠️ HIGH: `MainViewModel.RunCMDFromServerAsync` chạy trên thread nền của `System.Timers.Timer.Elapsed` (không phải UI thread), đọc trực tiếp `RegisterStatuses.FirstOrDefault(...)` (ObservableCollection) KHÔNG qua `Dispatcher.UIThread` trong khi luồng khác `Insert`/`RemoveAt` cùng collection trên UI thread → `InvalidOperationException: Collection was modified; enumeration operation may not execute`. Chỉ dispatch phần WRITE (set property INPC) là chưa đủ — phần READ (foreach/LINQ) cũng phải qua `Dispatcher.UIThread.InvokeAsync`. Đừng nhầm `System.Timers.Timer` (chạy ThreadPool) với `DispatcherTimer` (chạy UI thread)

### dotnet-general/
- [config-folder-missing-directorynotfoundexception.md](dotnet-general/config-folder-missing-directorynotfoundexception.md) — `File.WriteAllText` không tự tạo thư mục cha: `GetAppConfig()` crash `DirectoryNotFoundException` khi `config/` chưa tồn tại (máy mới/build output mới). Fix tại gốc: `SettingPathManagement.SaveConfigFolder` đổi field→property, tự `Directory.CreateDirectory` thư mục "config" ngay khi gán, che phủ mọi `File.WriteAllText("config/*.txt", ...)` phía sau
- [rabbitmq-basicpublish-blocks-during-autorecovery-hangs-ui.md](dotnet-general/rabbitmq-basicpublish-blocks-during-autorecovery-hangs-ui.md) — ⚠️ HIGH: `RabbitMqEventBus.Send()` chỉ check `channel == null`, nhưng `AutorecoveringModel` (AutomaticRecoveryEnabled=true) không bao giờ null kể cả khi mất kết nối → `BasicPublish` block đồng bộ chờ auto-recovery, treo cả method gọi (VD `MainViewModel.OnEventSendToServerAsync`) TRƯỚC khi tới code cập nhật UI phía sau — không exception, không log, ảnh hưởng mọi loại thiết bị dùng chung method. Fix: guard `IsConnected` trong `Send()` + fire-and-forget ở tầng gọi
- [newtonsoft-rectangle-serialized-as-string.md](dotnet-general/newtonsoft-rectangle-serialized-as-string.md) — ⚠️ HIGH: Newtonsoft ưu tiên `TypeConverter` nên `System.Drawing.Rectangle` được lưu thành CHUỖI `"1240, 880, 144, 192"`, KHÔNG phải object `{"X":…}`. Migrate sang kiểu tự viết (bỏ System.Drawing để cross-platform) → deserialize ra `default` (0,0,0,0) hoàn toàn IM LẶNG, mất sạch cấu hình cũ mà không có exception/log. Fix: `JsonConverter<T>` giữ đúng định dạng chuỗi + probe round-trip trên CSDL THẬT. Nhóm kiểu hay dính: Rectangle, Point, Size, Color, Guid, TimeSpan, Version
- [kztek-object-entity-vs-multyplatform-cs0433-type-collision.md](dotnet-general/kztek-object-entity-vs-multyplatform-cs0433-type-collision.md) — Thêm `Kztek.Cameras.Avalonia` vào project đang chạy tốt → CS0433 hàng loạt ở các file GHI LOG (không liên quan camera): `Kztek.Object.MultyPlatform` giữ nguyên namespace/tên kiểu của `Kztek.Object.Entity` (`SystemUtils`/`SystemLog`/`EmSystemActionType`). Fix: gom mọi điểm chạm vào 1 lớp bọc `CoreLog` trong project chỉ tham chiếu Entity, thay vì `extern alias` rải khắp nơi. Kèm bẫy CS0118 khi đặt namespace `*.Camera` cạnh kiểu `Kztek.Cameras.Camera`
- [tbl-led-getdata-crash-on-null-columns.md](dotnet-general/tbl-led-getdata-crash-on-null-columns.md) — ⚠️ HIGH: iPGSv4 `tblLed.GetData()` parse cứng `int.Parse` trên cột NULL-able (`zero_color`, `led_function`, `number_of_line`, `type_detail`) → seed dữ liệu bằng SQL mà để NULL làm app CHẾT IM LẶNG lúc khởi động (exception trong `LoadingViewModel` chạy nền, không popup, tiến trình biến mất). Schema NULL-able ≠ code chịu được NULL — phải đọc `GetData()` trước khi seed; bắt app chết im bằng `cmd /c app.exe > out.txt 2>&1`
- [git-ref-file-corrupted-null-bytes-recovery.md](dotnet-general/git-ref-file-corrupted-null-bytes-recovery.md) — ⚠️ HIGH: `.git/refs/heads/<branch>` hỏng thành 41 byte NUL sau crash → `fatal: your current branch appears to be broken`, mọi lệnh git fail. Commit **KHÔNG mất** (object độc lập với ref): lấy SHA từ file `.git/logs/refs/heads/<branch>` → `rm` file ref hỏng (BẮT BUỘC, `update-ref` không lock được file hỏng) → `git update-ref`. KHÔNG clone lại / KHÔNG `reset --hard`
- [dotnet-sln-rid-build-va-slnx-mac-dinh.md](dotnet-general/dotnet-sln-rid-build-va-slnx-mac-dinh.md) - `dotnet build <sln> -r <RID>` bi chan boi NETSDK1134 (RID la thuoc tinh cap PROJECT, khong phai solution); `dotnet new sln` tren SDK 10 mac dinh sinh `.slnx` chu khong phai `.sln` (dung `--format sln`); `dotnet sln add` tu keo theo moi project duoc tham chieu
- [newtonsoft-multi-ctor-no-default-deserialize-throw.md](dotnet-general/newtonsoft-multi-ctor-no-default-deserialize-throw.md) — ⚠️ HIGH: Newtonsoft.Json throw `JsonSerializationException` khi deserialize class có ≥2 constructor có tham số và không có parameterless/`[JsonConstructor]` — dù có public property setters; chỉ lộ runtime khi data thật chảy qua (stub trả null không trigger). Fix: `JObject.Parse` + map thủ công vào constructor đúng khi không sửa được model; hoặc `[JsonConstructor]` nếu sửa được
- [dotnet-no-implicitusings-task-not-found.md](dotnet-general/dotnet-no-implicitusings-task-not-found.md) — Class library cũ không có `<ImplicitUsings>enable</ImplicitUsings>` → `Task<>` không tìm thấy: thêm `using System.Threading.Tasks;` tường minh vào file, KHÔNG sửa csproj
- [kpi-counter-transient-status-race-always-zero.md](dotnet-general/kpi-counter-transient-status-race-always-zero.md) — KPI dashboard đếm theo status string set-rồi-reset trong CÙNG 1 method đồng bộ (timer A) luôn ra 0 khi timer B (refresh KPI) đọc độc lập — trạng thái transient không tồn tại đủ lâu để bắt được; fix bằng đếm từ nguồn bền (VD `ConcurrentQueue.Count`) thay vì đếm status hiển thị tạm thời
- [swallowed-exception-leaves-cmd-status-stuck-in-progress-forever.md](dotnet-general/swallowed-exception-leaves-cmd-status-stuck-in-progress-forever.md) — ⚠️ HIGH: `catch (Exception) { }` rỗng ở timer cha nuốt exception xảy ra GIỮA lúc set DB Status=DANG_THUC_HIEN và set THANH_CONG/LOI → dòng LogCmd kẹt Status=1 vĩnh viễn dù đã dequeue khỏi RAM queue (trông như "xử lý xong"), bị requeue lại y hệt mỗi lần app restart; verify bằng cách đọc trực tiếp SQLite qua python thay vì chỉ đọc code
- [dotnet-linux-compat-tfm-resx-systemdrawing.md](dotnet-general/dotnet-linux-compat-tfm-resx-systemdrawing.md) — Linux compat: đổi TFM net8.0, loại bỏ System.Drawing, exclude .resx WinForms sót lại
- [git-bash-detection-windowsapps-wsl-symlink-stub.md](dotnet-general/git-bash-detection-windowsapps-wsl-symlink-stub.md) — Tự dò `bash.exe` qua PATH trúng nhầm `WindowsApps\bash.exe` (symlink → `wsl.exe`) trên máy có WSL — path MSYS `/e/...` fail vì WSL cần `/mnt/e/...`; fix bằng derive từ vị trí `git.exe` + loại trừ `WindowsApps` khỏi fallback quét PATH
- [iparkingv8-api-systemutils-logger-null.md](dotnet-general/iparkingv8-api-systemutils-logger-null.md) — Dùng `IParkingv8.API` từ project mới luôn crash NullReferenceException nếu quên `SystemUtils.logger = LoggerFactory.CreateLoggerService(...)` trước khi gọi API
- [dotnet-baselib-broken-projectref-prebuilt-dll.md](dotnet-general/dotnet-baselib-broken-projectref-prebuilt-dll.md) — BaseLIB ProjectReference bị broken: switch sang `<Reference><HintPath>` pre-built DLL; System.Drawing.Primitives (Point/Size) là cross-platform nhưng System.Drawing.Common (Image/Bitmap/Graphics) thì không; cascade check sau `<Compile Remove>`
- [netframework-eventhandler-requires-eventargs-constraint.md](dotnet-general/netframework-eventhandler-requires-eventargs-constraint.md) — Port class dùng `EventHandler<T>` từ .NET 8 sang .NET Framework: mscorlib có ràng buộc `where TEventArgs : EventArgs` (đã bị gỡ trên .NET Core/.NET 5+) → CS0311 nếu class không kế thừa `EventArgs`
- [github-push-rejected-file-over-100mb-native-binary.md](dotnet-general/github-push-rejected-file-over-100mb-native-binary.md) — `git push` bị GitHub chặn cứng vì native binary (.so) > 100MB đã lỡ commit; phải `git filter-branch` xóa khỏi TOÀN BỘ lịch sử (không chỉ commit mới) + cẩn thận `refs/stash` giữ tham chiếu tới blob cũ nếu đã stash trước khi rewrite
- [msbuild-obj-filelistabsolute-stale-conflict-marker.md](dotnet-general/msbuild-obj-filelistabsolute-stale-conflict-marker.md) — `MSB3541: Files has invalid value "<<<<<<< HEAD"` không nằm trong .csproj mà trong file cache `obj/*.csproj.FileListAbsolute.txt` sót conflict marker git — xóa file cache này (an toàn, tự sinh lại), kiểm tra mọi project trong solution
- [rabbitmq-client-v7-recovery-event-api.md](dotnet-general/rabbitmq-client-v7-recovery-event-api.md) — RabbitMQ.Client v7: `RecoverySucceededAsync`/`ConnectionRecoveryErrorAsync` nằm TRỰC TIẾP trên `IConnection` (không cần cast `IAutorecoveringConnection`); handler signature phải là `AsyncEventArgs` (RabbitMQ.Client.Events), KHÔNG phải `EventArgs`; với `TopologyRecoveryEnabled=true` consumer TỰ ĐỘNG re-subscribe sau recovery
- [retype-int-field-to-enum-circular-projectref.md](dotnet-general/retype-int-field-to-enum-circular-projectref.md) — Đổi `LogEventVerify.Event/VerifyType` từ int sang enum: enum `EmTypeEvent` nằm ở project consumer (`IAccessv2.Objects`) trong khi field ở project lõi (`Kztek.Object.Entity`) → circular ProjectReference; fix bằng move enum xuống project lõi. Kèm cảnh báo: field `Event` chứa 2 domain giá trị khác nhau tuỳ driver (EmTypeEvent 0/1 vs raw code hãng 1/27) — chỉ đổi kiểu giữ nguyên giá trị bằng cast, không tự sửa logic khi chưa được user xác nhận
- [rabbitmq-client-v6-recovery-event-name-recoverysucceeded.md](dotnet-general/rabbitmq-client-v6-recovery-event-name-recoverysucceeded.md) — RabbitMQ.Client v6.x (sync API): event auto-recovery trên `IAutorecoveringConnection` tên là `RecoverySucceeded` (EventHandler&lt;EventArgs&gt; thường), KHÔNG phải `Recovery`; verify tên event 1 version cụ thể bằng reflect thẳng DLL trong `.nuget/packages` thay vì đoán; lọc `ShutdownEventArgs.Initiator == Application` để không cảnh báo giả khi tự Dispose()
- [wsl-linux-deb-build-global-json-sdk-version-mismatch.md](dotnet-general/wsl-linux-deb-build-global-json-sdk-version-mismatch.md) — Build `.deb` qua WSL báo "A compatible .NET SDK was not found" dù đã cài dotnet: `global.json` pin SDK version (VD 9.0.313) độc lập với `TargetFramework` (net8.0) trong .csproj — phải đọc `global.json` để biết channel cần cài, không suy luận từ TargetFramework
- [wsl-required-for-linux-native-tools-not-git-bash.md](dotnet-general/wsl-required-for-linux-native-tools-not-git-bash.md) — Git Bash for Windows (MSYS) KHÔNG BAO GIỜ có `dpkg-deb`/`apt`/tool Linux-native dù detect đúng bash.exe — script cần tool đó PHẢI chạy qua `wsl.exe` thật + path `/mnt/<ổ>/...`; dotnet trong WSL phải symlink vào `/usr/local/bin` mới thấy được khi gọi non-interactive (`wsl.exe -e bash -c`)
- [wsl-hybrid-build-windows-projectref-msys-pathconv.md](dotnet-general/wsl-hybrid-build-windows-projectref-msys-pathconv.md) — ⚠️ HIGH: build `.deb` qua WSL fail 3 lớp: `ProjectReference` tuyệt đối Windows (`E:\...`) không hiểu được bởi dotnet Linux → publish PHẢI chạy dotnet.exe gốc Windows; quoting lồng 2 lớp qua `wsl.exe -- bash -lc "<script nhiều dòng>"` làm biến rỗng → tách ra file `.sh` riêng gọi `bash <file> <args>`; Git Bash tự dịch path `/mnt/e/...` thành path Windows khi gọi native exe → set `MSYS_NO_PATHCONV=1` trước lệnh `wsl.exe`
- [powershell-utf8-no-bom-string-parse-corruption.md](dotnet-general/powershell-utf8-no-bom-string-parse-corruption.md) — ⚠️ HIGH: file `.ps1` UTF-8 KHÔNG BOM chứa ký tự Unicode (em dash, tiếng Việt...) khiến Windows PowerShell 5.1 đọc nhầm theo ANSI codepage → string bị "cắt" giữa chừng, lỗi parser xuất hiện ở CÁC DÒNG XA không liên quan; fix bằng `Set-Content -Encoding UTF8` (PS 5.1 tự thêm BOM) rồi verify lại bằng `Parser::ParseFile`
- [projectreference-swap-drops-transitive-deps-silently.md](dotnet-general/projectreference-swap-drops-transitive-deps-silently.md) — Đổi `ProjectReference` sang bộ project mới làm rớt type/package transitive không liên quan trực tiếp (Digest add-on, System.Drawing.Common, project khác); build lặp nhiều vòng từng project một (lá→gốc) thay vì build cả solution 1 lần để không bỏ sót lỗi tầng sâu
- [garbled-filename-msbuild-maxpath.md](dotnet-general/garbled-filename-msbuild-maxpath.md) — ⚠️ Lệnh `cp` chạy sai quoting trên Windows/Git Bash biến cả dòng lệnh thành 1 tên file siêu dài (NTFS map ký tự đặc biệt sang Private Use Area) nằm trong thư mục bị `<AvaloniaResource Include="Assets\**">` glob → MSBuild crash "exceeds the OS max path limit", chặn toàn bộ build; xóa bằng Python với prefix `\\?\` (extended-length path), không dùng `os.remove` thường vì path >260 ký tự
- [dpapi-protecteddata-cross-platform-secret-migration.md](dotnet-general/dpapi-protecteddata-cross-platform-secret-migration.md) — ⚠️ HIGH: DPAPI `ProtectedData` chỉ chạy Windows — app cross-platform PHẢI xử lý Linux tường minh (cảnh báo 1 lần, KHÔNG im lặng plaintext); pattern prefix `enc:v1:` + migrate lazy plaintext cũ + fail-soft (decrypt fail trả rỗng, không mất cả file profile)
- [ssh-shell-quote-whitelist-printf-xclip-gotchas.md](dotnet-general/ssh-shell-quote-whitelist-printf-xclip-gotchas.md) — ⚠️ HIGH: bộ gotcha chạy lệnh shell từ .NET: whitelist filename phải cho phép `~`/`%` (version chuẩn Debian `.deb`); `printf '%s\n' '<arg>'` an toàn với `%` ở vị trí argument (chỉ nguy hiểm khi vào format string); `xclip` daemonize → dispose Process ngay, KHÔNG WaitForExit/kill tree (mất clipboard)
- [csproj-absolute-projectref-fixed-relative-wsl-build.md](dotnet-general/csproj-absolute-projectref-fixed-relative-wsl-build.md) — `ProjectReference` tuyệt đối `E:\KZTEK\...\KztekComponentAvalonia.csproj` (4 file: IPGS.Controls, IPGSUseCam, ApplicationConfig, Kztek.Cameras.Avalonia) khiến dotnet WSL "Skip" project → hàng loạt `CS0246` type not found downstream; sửa sang path tương đối AN TOÀN vì cùng 1 máy dev + đã verify 0 Error cả Windows lẫn WSL — đối lập có chủ đích với [wsl-hybrid-build-windows-projectref-msys-pathconv.md](dotnet-general/wsl-hybrid-build-windows-projectref-msys-pathconv.md)
- [stale-json-snapshot-cache-hides-newly-registered-record.md](dotnet-general/stale-json-snapshot-cache-hides-newly-registered-record.md) — Field cache gán lại bằng `JsonConvert.DeserializeObject<T>(paramJson)` bên trong 1 method ghi là snapshot ĐÔNG CỨNG, không đồng bộ với state gốc sau đó — method READ dùng lại field này (không nhận tham số mới) đọc dữ liệu cũ nếu không có method ghi nào khác chạy lại; fix bằng cho method READ nhận tham số JSON tươi tại thời điểm gọi thay vì dựa cache nội bộ
- [console-outputencoding-throws-when-stdout-redirected.md](dotnet-general/console-outputencoding-throws-when-stdout-redirected.md) — Gán `Console.OutputEncoding = UTF8` **ném IOException** khi stdout bị redirect (`Start-Process -RedirectStandardOutput`, WinExe) ⇒ gộp cùng `try` với `Console.SetOut(...)` khiến SetOut không bao giờ chạy, log tiếng Việt ra file thành `?` mà không exception; fix: tách 2 try/catch, ưu tiên `Console.SetOut(new StreamWriter(Console.OpenStandardOutput(), UTF8){AutoFlush=true})`. Bẫy kèm: `Get-Content` PS 5.1 mặc định đọc ANSI nên dễ "fix" nhầm phía app

### camera-integration/
- [kztek-cameras-disposes-event-frame-after-invoke.md](camera-integration/kztek-cameras-disposes-event-frame-after-invoke.md) — ⚠️ HIGH: `AnvPlayerService` gọi `boxedFrame.Dispose()` trong `finally` ngay sau `AIDetectAnalyzing?.Invoke` với comment "không có subscriber nào (đã kiểm tra)" — subscriber ĐẦU TIÊN xuất hiện là mọi ảnh sự kiện thành **0 byte**, `WriteableBitmap.Save()` KHÔNG ném lỗi. Handler `async void` trả về ở await đầu tiên nên `finally` chạy trước khi xử lý xong → phải sao chép pixel ngay dòng đầu, trước cả 1 câu SQL. Comment "đã kiểm tra không ai dùng" là bom hẹn giờ
- [kztek-cameras-start-signature-drift.md](camera-integration/kztek-cameras-start-signature-drift.md) — `Kztek.Cameras.Camera.Start()` đổi chữ ký (bỏ `enableMotionDetection` + `cameraSDK`, tách `from/toMotionAlarmLevel`, thêm `EmVirtualLoopType`) → project consumer lâu không build bị hỏng ngầm; luôn `dotnet build` TRƯỚC khi sửa project tham chiếu repo `0.BaseLIB` dùng chung, dùng named argument cho tham số optional
- [camera-type-int-decoded-with-wrong-enum.md](camera-integration/camera-type-int-decoded-with-wrong-enum.md) — ⚠️ CRITICAL: `camera.Type` (int) bị decode bằng SAI enum (`Kztek.Cameras.CameraType` thay vì decoder gốc `Camera.GetCameraType()`) — 3 enum "loại camera" khác thứ tự cùng tồn tại trong repo khiến camera Custom bị hiểu nhầm thành HANET, build sai RTSP URL, treo "Connecting" mãi không log lỗi
- [readpl-api-changed-skbitmap-not-systemdrawing.md](camera-integration/readpl-api-changed-skbitmap-not-systemdrawing.md) — ReadPL 1.0.8 (Multi-Platform) đổi API `Recognize()`/`Plate.Crop` sang thẳng `SkiaSharp.SKBitmap`, không còn `System.Drawing.Bitmap`/`Image` như bản 1.0.3 — comment code cũ lỗi thời, dùng reflection trên DLL để xác nhận API thật
- [kztek-cameras-avalonia-getframe-returns-skbitmap.md](camera-integration/kztek-cameras-avalonia-getframe-returns-skbitmap.md) — ⚠️ CRITICAL: `Camera.GetCurrentVideoFrameAsync()` (Kztek.Cameras external lib) trả `SKBitmap` **tham chiếu sống** (`_currentSKFrame`) — TUYỆT ĐỐI KHÔNG dispose, nếu không sẽ gây `AccessViolationException` tại `SKBitmapPool.Return()`; cần bản riêng thì `.Copy()` trước
- [milesight-sdk-integration.md](camera-integration/milesight-sdk-integration.md) — Tích hợp SDK Milesight: cấu hình, port, channel
- [yt-dlp-youtube-stream-cv2.md](camera-integration/yt-dlp-youtube-stream-cv2.md) — YOLO detect từ link YouTube: yt-dlp `extract_info(download=False)` lấy direct stream URL rồi mới đưa vào `cv2.VideoCapture`; resolve trong thread nền tránh đứng UI
- [ffmpeg-linux-nativelibrary-load-fullpath-still-needs-ld-library-path.md](camera-integration/ffmpeg-linux-nativelibrary-load-fullpath-still-needs-ld-library-path.md) — `NativeLibrary.Load(fullpath)` nạp lib FFmpeg đầu tiên OK (không cần LD_LIBRARY_PATH), nhưng dependency NỘI BỘ giữa các .so (libavformat cần libavcodec/libavutil...) do ld.so resolve — CHỈ tìm theo LD_LIBRARY_PATH, không tự biết thư mục vừa nạp lib đầu; verify bằng `ldd`, fix trong run.sh
- [public-traffic-cam-find-direct-stream-url.md](camera-integration/public-traffic-cam-find-direct-stream-url.md) — Link trang aggregator camera (opencctv.org...) không mở được bằng cv2; tìm link "Feed URL" có sẵn trên trang hoặc DevTools Network→hls, ưu tiên domain gốc (vd wzmedia.dot.ca.gov) hơn proxy riêng của aggregator
- [lpr-engine-swap-iparkingv5-to-parkingv8-skiasharp.md](camera-integration/lpr-engine-swap-iparkingv5-to-parkingv8-skiasharp.md) — Đổi engine LPR `iParkingv5.Lpr` (Windows-only, thiếu HawkeyeCS.dll) → `ParkingV8.Lpr` (cross-platform, ReadPL+SkiaSharp): `ILpr` nhận thẳng SKBitmap (bỏ bridge System.Drawing), chỉ còn `GetPlateNumberAsync` (bỏ bản sync), `LprFactory.CreateLprDetecter` còn 1 tham số, `LoggerFactory` đổi namespace con
- [ipgs-camera-type-milesight-no-rtsp-builder.md](camera-integration/ipgs-camera-type-milesight-no-rtsp-builder.md) — Map `IPGS.Object` CameraType (HIKVision/Dahua/Milesight) → `Kztek.Cameras.CameraType`: HIKVision phải map `HIKVISION2` (không phải `HIKVISION` — không có builder); Milesight KHÔNG có builder nào, phải dùng `Custom` + tự build URL RTSP thủ công
- [ipgs-camera-controller-stale-channel-after-edit.md](camera-integration/ipgs-camera-controller-stale-channel-after-edit.md) — Sửa camera đang chạy (Channel/IP/Port/User/Pass) không có tác dụng: `ICameraController` (Hik/Dahua/Milesight) chỉ đọc `Channel` MỘT LẦN trong constructor, không có chỗ nào refresh lại — phải Stop()+Start() lại camera sau khi lưu edit để tạo controller mới
- [anvplayerservice-motionanalyzing-unsubscribed-bitmap-leak.md](camera-integration/anvplayerservice-motionanalyzing-unsubscribed-bitmap-leak.md) — ⚠️ CRITICAL: `AnvPlayerService.ConsumerMotionDetectionAsync/ConsumerAiDetectionAsync` cấp phát `bmp.Copy()` cho event `MotionAnalyzing`/`AIDetectAnalyzing` KHÔNG CÓ subscriber nào trong app → rò rỉ native SKBitmap mỗi lần phát hiện biển số/AI mới khi camera thật đang chạy (không xảy ra khi test không camera)
- [anvplayer-lastframe-dead-field-watchdog-false-stop.md](camera-integration/anvplayer-lastframe-dead-field-watchdog-false-stop.md) — ⚠️ CRITICAL: `VideoStreamDecoderIntptr._lastFrame` tồn tại (dùng bởi `GetCurrentFrame(w,h)`) nhưng KHÔNG BAO GIỜ được gán frame thật (`CloneFrame()` có định nghĩa, 0 call site) — mọi API/watchdog dựa vào field này luôn thấy null → `frmViewCamera` đứng hình sau 8s do watchdog false-positive tự dừng player; fix bằng populate `_lastFrame` qua `CloneFrame(_pFrame)` (av_frame_ref, rẻ) ngay trong decode loop
- [camera-ai-active-code-static-readonly-never-set.md](camera-integration/camera-ai-active-code-static-readonly-never-set.md) — ⚠️ CRITICAL: `Kztek.Cameras.Camera.AI_ACTIVE_CODE` (static field) chưa từng được gán ở app thật (chỉ gán tay qua UI trong tool test nội bộ) → `AnvPlayerService.plateReader` (`static readonly`, chỉ đọc field 1 lần lúc class chạm đầu tiên) luôn khởi tạo activation code rỗng → ReadPL chạy degraded mode im lặng, không detect, không exception; fix: gán `Camera.AI_ACTIVE_CODE = configuration.Lpr.AIActiveCode` ngay sau khi load config, TRƯỚC MỌI code chạm namespace Kztek.Cameras
- [camera-decode-uniform-fit-vs-render-stretch-fill-blur.md](camera-integration/camera-decode-uniform-fit-vs-render-stretch-fill-blur.md) — ⚠️ CRITICAL: Decode dùng Uniform-fit (giữ aspect ratio) nhưng `Render()` vẽ Stretch=Fill (lấp đầy control) → 2 lớp scale không khớp (sws_scale downscale rồi Avalonia DrawImage upscale lại) → ảnh mờ + độ phân giải hiển thị không đúng thực tế; fix: đổi decode sang Fill khớp `ctrlW×ctrlH` để `sws_scale` là bước resize duy nhất
- [channel-reader-stuck-missing-trycomplete-on-dispose.md](camera-integration/channel-reader-stuck-missing-trycomplete-on-dispose.md) — ⚠️ HIGH: `VideoStreamDecoderIntptr.Dispose()` không gọi `TryComplete()` trên 3 channel writers → khi RTSP drop+reconnect, consumer `ConsumerDisplayFrameAsync` bị stuck mãi trong `ReadAllAsync` của channel cũ (chưa completed, token chưa cancelled) → UI đứng hình vô thời hạn dù stream mới đã kết nối. Fix: 3 dòng `TryComplete()` vào đầu Dispose()
- [x11-xshmattach-async-baderror-crashes-process.md](camera-integration/x11-xshmattach-async-baderror-crashes-process.md) — ⚠️ CRITICAL: `XShmAttach` bất đồng bộ — trả `true` ngay, lỗi `BadAccess` đến SAU qua X error event; Xlib default handler gọi `exit()` trước khi fallback C# kịp chạy; fix: `XSetErrorHandler` tùy chỉnh (delegate `static readonly`) + reset flag + `XSync()` sau `XShmAttach` để buộc lỗi flush về ngay
- [x11-screencapturer-stale-screensize-on-resolution-change.md](camera-integration/x11-screencapturer-stale-screensize-on-resolution-change.md) — `XDisplayWidth`/`XDisplayHeight` là macro cache client-side KHÔNG detect live RandR change → BadMatch vô hạn sau xrandr rotate; PHẢI dùng `XGetGeometry` (real round-trip) throttle mỗi N frame, kết hợp CleanupSHM+TryInitSHM khi phát hiện đổi
- [lao-plate-read-lao-letters-loop-position.md](camera-integration/lao-plate-read-lao-letters-loop-position.md) — Đọc biển số Lào: ນ (loop TRÊN-trái) vs ມ (loop DƯỚI-trái) vs ບ (không loop), ຫ (2 loop, 2 bụng) vs ທ (1 loop) — phải crop + upscale LANCZOS x4-x8 và render đối chiếu bằng font `LeelaUIb.ttf` trước khi kết luận; dòng chữ nhỏ trên biển là tên tỉnh (`ນະຄອນຫຼວງວຽງຈັນ`), không thuộc số đăng ký

### networking-protocol/
- [blocking-native-call-in-receive-loop-trips-independent-heartbeat.md](networking-protocol/blocking-native-call-in-receive-loop-trips-independent-heartbeat.md) — Lệnh X11 `XSync` đồng bộ trong vòng đọc message (mouse/keyboard inject) chặn luôn việc nhận Pong, khiến watchdog heartbeat (chạy task riêng, tưởng đã an toàn) vẫn tưởng nhầm mất kết nối; fix bằng Channel<Action> tách inject ra task riêng
- [zktecopull-connect-ack-error-firmware-behavior.md](networking-protocol/zktecopull-connect-ack-error-firmware-behavior.md) — ZKTeco C3 firmware LUÔN trả `ACK_ERROR(0xC9)` cho `CMD_CONNECT` kể cả khi connected, kèm session ID trong payload — firmware behavior CÓ CHỦ Ý (ZKTecoCore accept cả hai từ đầu); ADR fix quá strict cho CONNECT → accept cả hai; xác nhận bằng hex dump 4/4 variant
- [tcp-async-write-timeout-watchdog-separate-task.md](networking-protocol/tcp-async-write-timeout-watchdog-separate-task.md) — ⚠️ HIGH: heartbeat/watchdog đặt CÙNG loop với `WriteAsync` không timeout → slow-reader chặn write vô hạn, timeout không bao giờ fire (DoS chiếm slot session); `NetworkStream.WriteTimeout` KHÔNG áp dụng cho async write — tách watchdog task riêng + `CancelAfter` mọi WriteAsync
- [port-legacy-namespace-mismatch.md](networking-protocol/port-legacy-namespace-mismatch.md) — Port class sang project mới, tưởng namespace theo đường dẫn thư mục nhưng file cũ đã copy-paste nguyên `namespace` từ project nguồn — luôn Read file thật trước khi viết `using`
- [kz-led-p5-udp-outdoor-integration.md](networking-protocol/kz-led-p5-udp-outdoor-integration.md) — Thêm bảng LED ngoài trời P5 (UDP string protocol) vào IPGSv1: hạ tầng `OutDoorLed`/`UdpTools`/`CmdBuilder` đã generic, chỉ cần thêm `EM_ModuleType.P5` + vài case switch, KHÔNG viết class mới; FontSize P5 (10/20/30) khác hẳn thang point-size P10 dù trùng số 10 — phải lọc riêng theo module type ở UI
- [dotnet-ipaddress-tryparse-bare-number.md](networking-protocol/dotnet-ipaddress-tryparse-bare-number.md) — `IPAddress.TryParse("254")` trả `true` (cú pháp IPv4 kiểu cũ, tự expand `0.0.0.254`) — không dùng được làm bộ phân loại "IP đầy đủ vs octet rút gọn"; phải tự kiểm tra ký tự `.` trong chuỗi TRƯỚC khi quyết định nhánh parse
- [socket-accept-close-wsacancelblockingcall.md](networking-protocol/socket-accept-close-wsacancelblockingcall.md) — `Socket.Accept()` blocking chạy trong `Task.Run` + đóng listener để hủy → ném `SocketException` (WSACancelBlockingCall trên Windows), không phải `OperationCanceledException` — VS debugger break "User-Unhandled" dù đây là luồng dừng server bình thường; bắt riêng `SocketException` khi `ct.IsCancellationRequested`
- [reverse-proxy-breaks-vendor-software-remote-command-source-ip-routing.md](networking-protocol/reverse-proxy-breaks-vendor-software-remote-command-source-ip-routing.md) — Reverse-proxy HTTP forward nguyên byte (verify sha1 khớp tuyệt đối) vẫn làm hỏng tính năng "gửi lệnh tức thời" (Remote Opening/Sync Time) của ZKBioAccess — phần mềm hãng định tuyến lệnh theo IP NGUỒN kết nối (đổi thành 127.0.0.1 qua proxy) chứ không chỉ theo SN, nên lệnh không khớp được vào đúng phiên polling; dấu hiệu nhận biết: `Return Time - Submit Time` CỐ ĐỊNH tuyệt đối (~30s) mọi lần thử = timeout nội bộ, không phải phản hồi thiết bị thật; luồng đẩy dữ liệu lên (registry/cdata/push) không bị ảnh hưởng
- [npcap-windows-loopback-dlt-null-lso-gso.md](networking-protocol/npcap-windows-loopback-dlt-null-lso-gso.md) — ⚠️ HIGH: SharpPcap/Npcap bắt gói tin trên LOOPBACK Windows dùng `LinkLayers.Null` (BSD family header 4-byte, KHÔNG phải Ethernet); payload lớn (~200KB) qua loopback bị Windows LSO/GSO ghi sai IP Total Length = 0xFFFF → PacketDotNet đọc vượt mảng ném `IndexOutOfRangeException`. Fix ĐÚNG: vá lại 2 byte Total Length ngay trên `raw.Data` gốc (giữ nguyên LinkLayers.Null) — KHÔNG tự bóc byte + đổi sang `LinkLayers.Raw` (PacketDotNet đã tự xử lý đúng Null, tự diễn giải lại gây đọc sai offset cờ SYN/FIN/RST → 1 kết nối thật bị tách thành nhiều "connection" giả). Chỉ ảnh hưởng test bằng loopback + payload lớn bất thường, thiết bị thật qua NIC vật lý không gặp
- [enum-cast-raw-protocol-code-breaks-shared-severity-mapper.md](networking-protocol/enum-cast-raw-protocol-code-breaks-shared-severity-mapper.md) — ⚠️ HIGH: E02/E05/ELV485 (legacy + Avalonia) ép raw code protocol (1/27) vào `EmTypeEvent` (chỉ Deny=0/Grant=1) theo "parity nguồn" — số 1 trùng `Grant` nhưng SAI Ý NGHĨA (thẻ lạ/denied lại thành Grant); rủi ro từng bị đánh giá "chỉ ảnh hưởng display" hết hiệu lực ngay khi `EventSeverityMapper` (thêm cùng ngày) đọc trực tiếp raw int đó → sai màu severity UI thật. Fix: gán thẳng tên enum theo ý nghĩa Grant/Deny thật, không cast số

### database/
- [sqlite-schema-migration-skipped-when-db-already-exists.md](database/sqlite-schema-migration-skipped-when-db-already-exists.md) — ⚠️ HIGH: `SqliteHelper` ctor chỉ gọi `TblEvent.CreateTable()` (thêm cột mới) bên trong `if (!File.Exists(dbPath))` → migration KHÔNG BAO GIỜ chạy trên DB đã tồn tại từ trước (đúng đối tượng cần migrate nhất); dev test luôn thấy pass vì DB dev luôn mới toanh. Fix: tách `EnsureOutboxColumns()` riêng, gọi ở CẢ nhánh else (DB cũ) lẫn trong CreateTable() (DB mới)
- [sql-injection-legacy-filldata.md](database/sql-injection-legacy-filldata.md) — SQL injection trong legacy FillData() pattern: escape `.Replace("'", "''")` khi không thể parameterize ngay
- [sqlclient-migration-ssl-cert-trust-error.md](database/sqlclient-migration-ssl-cert-trust-error.md) — Đổi `System.Data.SqlClient` → `Microsoft.Data.SqlClient`: build pass nhưng runtime lỗi "certificate chain was issued by an authority that is not trusted" — phải thêm `TrustServerCertificate=True` vào mọi connection string (SQL Server on-prem self-signed cert)
- [mssql-server-ubuntu-repo-missing-signed-by.md](database/mssql-server-ubuntu-repo-missing-signed-by.md) — Cài SQL Server/mssql-tools trên Ubuntu 22.04 báo `NO_PUBKEY` dù key đã import đúng — file `.list` Microsoft cung cấp thiếu `signed-by=`, phải tự thêm trỏ vào keyring đã dearmor

### ui-patterns/
- [kznumericupdown-fixed-width-column-overflow.md](ui-patterns/kznumericupdown-fixed-width-column-overflow.md) — `KzNumericUpDown` (horizontal) có 3 phần Width cố định (45+80+45=170px), không co giãn — nhét vào cột Grid hẹp hơn 170px sẽ tràn ra ngoài border, không có exception/warning nào cảnh báo
- [avalonia-localization-typed-resource-keys.md](ui-patterns/avalonia-localization-typed-resource-keys.md) — Localization bằng magic string `Get("key")` dễ gõ sai/không dịch được — port sang property mạnh kiểu (`CurrentResources.FrmLogin`) như bản WinForms, JSON deserialize thẳng vào property qua reflection cache

---

## Cách đặt tên file lesson

```
[technology]-[problem-or-feature-slug].md
```

Ví dụ:
- `avalonia-data-binding-null-reference.md`
- `milesight-sdk-channel-numbering.md`
- `ef-core-migration-conflict.md`
- `modbus-tcp-reconnect-loop.md`
